# 一次性按比例减仓：审查与发布记录

日期：2026-09-06。基线：`bca3fb8` + `FIX_REVIEW_20260906.md` 中的四项修复。
状态：**本地实现、模拟盘及 Safari 验收完成；主审四轮检查结束（第二轮发现并修复一项问题，第三、四轮连续通过）。代码提交 `38ef62834a588c273c5c5daabdbac253136f22d5` 已推送，2026-09-07 已部署并启用；现场检查通过。**

## 生产发布（2026-09-07）

- 00:52 预检：39 个账本仓、39 个实仓及 39 张唯一市价止损一致，无孤儿、pending、残留。时区 Asia/Shanghai，备份任务未在运行。
- 第一次切换连接在 SSH 建连阶段超时；复查仍是旧 PID、旧源码、原 timer，尚未安装维护开关。未盲重试复制操作。
- 01:02:37 正常停止交易服务，先留存 root:root、0600 的源码与状态冷备 `/var/lib/trading-backups/pre-partial-close-20260907-0102.tar.gz`（297442 字节，归档检查及 sync 通过），再覆盖冻结发布包。配置、账本、止损日期的前后 SHA256 完全相同；不覆盖实际数据，不取消交易所止损。
- 首次启动设 `TRADING_DISABLE_NEW_OPENS=1`、`TRADING_ENABLE_PARTIAL_CLOSE=0`，核对全部 63 份清单文件、39 仓/39 张止损与 Web 接口后，才解除维护开关。
- 01:04:08 最终进程启动，MainPID `3148033`、单 worker；`TRADING_ENABLE_PARTIAL_CLOSE=1`，禁开仓及跳过日检标志均未设置。永久开关仅为 `/etc/systemd/system/trading.service.d/20-partial-close.conf` 的一条 Environment；临时维护文件已撤出 systemd。此次在日检前上线，不吞掉今天 08:00 日检。
- 实际本地 HTTP `/`、`/static/app.js`、`/api/status`、求索指数 OHLC 均为 200，新 JS 哈希一致，状态无异常；公网无认证访问返回既有 Cloudflare Access 的 302 登录跳转，不把它写成已登录的端到端验收。
- 最终再次核对 39 仓、39 张市价止损一致；原配置和账本哈希不变，没有实盘减仓测试。两次正常启动均完成同步与调度，无 ERROR/CRITICAL；未重启资源监控、隧道或整台服务器。
- 01:26 备份 timer 已恢复，NEXT 为 **2026-09-07 07:50 CST**，未手动触发备份；交易、监控、隧道和 timer 均 active，根盘使用率 24%，可用 29G。07:50 定时备份和 08:00 日检的本日现场结果尚未发生，不能提前宣称通过。

发布后的审查文档同步不改变运行源码指纹。生产净增 **378 行**、测试净增 **731 行**（相对 `bca3fb8`，含空行/注释；文档和清单另计）。不增加依赖、自动恢复路径或部署状态机；遇到减仓结果不确定仍隔离并通知人工。

## 主审终审记录（2026-09-06 20:10）

四轮均由当前主审亲自完成，没有使用子线程裁决替代。范围覆盖全部自有运行源码、网页、状态持久化、定时任务、运维定义及测试覆盖；第三方图表库核对既有哈希，不宣称逐行审计供应商压缩源码。

| 轮次 | 审查重点与证据 | 裁决 |
| --- | --- | --- |
| 1 | 全量自有源码核读，检查策略→执行→止损→账本→Web→调度/运维依赖；386 项 Python、4 项前端测试及静态门禁通过 | 未发现 P0/P1 |
| 2 | 反向追踪所有下单、改撤止损及状态写入口，复核完整系统交互；386 项逆序回归通过；额外执行硬退出、并发提交、数量取整、剩余仓反手探针 | 发现 P1：限价止损被误认作市价保护，本轮不通过，连续通过计数清零 |
| 3 | 修复后重查全部调用边界、账本生命周期、通知/统计、旧策略与部署兼容性；389 项逆序回归、6 项独立对抗探针、4 项前端测试通过 | 未发现新增 P0/P1，第 1 轮连续通过 |
| 4 | 再反查整体失败/重启/回滚条件、前后端契约和发布清单；389 项固定种子乱序回归（禁止真实 HTTP）、6 项探针、4 项前端测试、编译/ruff/清单/差异检查通过 | 未发现新增 P0/P1，第 2 轮连续通过 |

独立探针在仓库外运行，未计入 389 项正式测试，也未增加生产代码：五个真实子进程硬退出切点、两个并发 HTTP 执行请求、预览后调整未来风险度、减仓后只平剩余量再按原风险开新仓、300 组数量取整组合，以及禁止回滚至不识别 pending 的旧代码。进程退出测试使用离线交易所替身，不冒充断电或交易所实测。

### 本轮唯一修复

旧 `_algo_order_matches` 只确认方向、reduce-only、触发价及张数，漏查 `slOrdPx`。主审离线复现：100 币多仓、触发价 80，原止损委托价被改为限价 79 后，预览仍放行，减仓成功至 70 并宣称保护完整。限价委托在跳价后可能无法成交，不能作为本系统要求的市价止损。

最小修复：在同一个匹配器要求原生 `slOrdPx == -1`；缺失或限价均不认可，复用原有 mismatch/pending 人工隔离，不自动改回、不另挂止损。生产净 **+3 行**（含注释），新增 **3 项**回归测试并补齐既有测试夹具的原生字段。官方字段语义已对照 [OKX API 文档](https://www.okx.com/docs-v5)；锁定 ccxt 4.5.64 的创建请求也确实发送 `slOrdPx=-1`。

修复后运行源码指纹（从清单筛选非 tests 的 Python/HTML/JS 行再做 SHA256）：`e2638d597bbc46088d6f4cf2eb8e16d33b0fd158e80eb15275858931db2fab9f`。四项原修复 + 减仓 + 本次校验的生产净增由 375 调整为 **378 行**，不是本轮又新增 378 行。

### 生产只读核验及发布边界

20:02–20:05 通过 SSH 只读核验：交易、资源监控和 Cloudflare 服务 active，重启计数均 0；Asia/Shanghai；根盘 24%，负载约 0.09。39 个账本仓与 39 个实际仓一致、39 张唯一 conditional 止损均为市价 `slOrdPx=-1`；无孤儿、pending 或止损残留。真实账户 net/cross。没有交易所写请求、服务器落地测试文件或服务重启。

生产被更新的两份文件与 Git 基线的哈希差异已逐项比较，仅为 T+1 的注释/日志措辞，未发现未纳入候选的交易逻辑改动。其余被更新运行文件与基线一致。9 月 7 日 00:52 再次核对全部运行文件，另有 `ma_cross_strategy.py` 一处旧重入文档字符串，已确认仅注释不同；39 仓与 39 张市价止损仍一致，三个服务正常。当前在今日 08:00 日检之前，无需设置跳过当日日检标志，必须保留正常日检。

部署仍须先关闭新开仓总闸、暂停备份 timer、停服务再覆盖源码；不覆盖配置/账本/历史数据。启动核验通过后才启用减仓并恢复正常开仓配置；若部署发生在日检之后，才使用当日补跑跳过标志，并不能把该临时配置永久留在下一次启动定义里。

记录但不扩码：pending 状态下部分统计/日报仍按最后已确认账本量展示，必须以异常提示及交易所核对为准；普通结果/周报/资源告警的失败送达并非全部有跨轮重试；历史归档落盘中断可能暂时重复展示记录。它们不用于开仓或自动解除隔离，本次不重构。市场流动性、滑点、交易所故障和整机断电并不因测试通过而获得绝对保证。

## 2026-09-06 模拟盘连接验证进展

用户提供专用模拟盘凭据并修正 IP 白名单后，带强制 `x-simulated-trading: 1` 的认证查询成功；账户为 `net_mode`、账户模式 2，返回权限字符串 `read_only,trade`。凭据仅经不回显终端进入临时验证进程内存，未写入源码、配置文件或本报告。

初次模拟开仓被 OKX 拒绝，返回 **50123：This API Key does not have trading permission for the Crypto**。该轮唯一发出的交易写请求为 BTC-USDT-SWAP 的单笔模拟开仓（SDK 通过 batch-orders 包装一笔，约 80 USDT 名义价值），未成交，收尾确认 BTC 模拟仓位和普通/算法委托均为空。

用户随后提供 OKX 完整授权说明：正向合约等“其他币对种类”须同时授权交易币和计价币。BTC 已勾选；用户补充 USDT 后，2026-09-06 19:29–19:31 的模拟盘交易验证成功，50123 障碍解除。没有要求全选币种或取消 IP 白名单，也没有更换实盘 Key、操作服务器。

### 基础模拟盘实测结果

通过本地真实 Flask 路由、减仓执行器、OKX 适配器及临时账本测试；不是只调用交易所裸接口。交易目标仅 BTC-USDT-SWAP，每次初始 0.10 张（0.001 BTC，约 80 USDT 名义金额），多空各一组。

- 两组都完成 30% 减仓：0.10 → 0.07 张；随后再请求减剩余仓位的 50%，按 0.01 张步长向下取整为 0.03 张，实际剩余 0.04 张。服务端预览显示实际数量，不承诺小仓位能精确按百分比成交。
- 每次普通减仓订单恰好发送一次；重复 token 和另一份旧预览均返回 409，没有追加订单。
- 每次减仓确认原止损 ID/触发价保持不变、原位数量匹配残仓、风险配置不变；同方向策略处理不发单、不补仓。
- 多头第二次减仓注入“交易所已接收普通订单，但客户端响应丢失”；空头第二次注入“止损改量已接收，但响应丢失”。均通过查询核对成功，没有重发减仓。
- 减仓完成后，另行把测试仓的止损价移近市价，观察剩余 0.04 张自然触发；多空均连续确认归零、无反向仓、无普通或算法挂单。**移近止损价只属于测试步骤，不是减仓功能的生产行为。**
- 本组会话共 291 次请求、16 次写请求；全程强制模拟盘标头、官方 HTTPS 主机和单品种写请求白名单。已退出会话，成功组临时账本已清除，没有真实钉钉推送。

本轮再次执行 Python 全量 386 项、Node 前端 4 项、ruff、清单校验及差异空白检查，均通过；生产源码没有因权限排查或本轮验收而修改。

### 改量失败时旧止损的真实行为

另用独立模拟仓，多空各验证一次：0.10 张先实际减仓至 0.07 张，在止损改量请求发送前注入网络异常。因此旧 conditional 止损仍是 0.10 张，而不是测试替身假称改量成功。

- 真实 HTTP 执行返回 503；重读磁盘账本仍有 pending，未写入猜测的成交归因。重复提交及系统全平均被阻断。
- 查原生清单确认旧止损 ID、原价、0.10 张仍在。随后仅为触发试验移近止损价，不修改其数量。
- 多空两张止损的原生详情均为 `state=effective`、`actualSz=0.07`；连续持仓查询为空，没有开出反向 0.03 张。两组结束后均无普通/算法挂单。
- 在此之前的一次多头触发等待超过 90 秒，**没有计通过**；原生详情证实 `triggerTime` 为空、`actualSz=0`、`failCode=0`，随后被测试清理平仓取消。缩小测试专用触发距离后重新完成上述多空验证，不是修改生产止损策略。

这为当前模拟盘净持仓、全仓、BTC 合约下的“旧止损超出残仓仍只减仓”提供了实测证据，不意味着能保证所有实盘网络/流动性/交易所故障。生产遇到 pending 仍须人工核对，不能因本次实测而取消隔离或增加自动恢复。

## 已实现的业务边界

- 100 张减仓 30%，按实际成交和交易规则留下剩余仓位；同方向日检不补回，反向时平剩余量，再按原风险配置开新仓。
- 不改风险度、入场时间、入场价、原止损价或策略参数。剩余仓位先触发止损时，沿用下一次日检按当前方向重入的规则。
- 仅接受 0% < 比例 < 100%；张数向下取整，减仓量与剩余量都满足交易所最小数量。不把小额减仓自动扩大成全平。
- 先做服务端预览，显示实际减仓量、剩余量与原止损价；用户再次输入品种名确认。签名预览有效期 120 秒，执行前重新核对实仓、原止损和仓位版本。
- 本次指令仅发送一次、仅允许 reduce-only。只受理不算成交；按唯一 clOrdId 查询终态并核对实仓，未成交部分不补单。未确认普通委托只撤本操作，绝不撤算法止损。
- 保留原止损 ID，原位只改数量，`cxlOnFail=false`。改量回执不算成功，必须查询确认。不会先撤旧止损再新建。
- 已实现减仓盈亏写入历史；完成仓位的胜率按同一仓位全部退出记录合计，不把减仓次数当作完整交易次数。手续费仍沿用原系统估算费率，非交易所精确对账。

## 不确定结果：人工隔离，不扩张自动恢复

只增加每个托管仓位内的一份 `pending_reduction` 和一个 `reduction_revision`，无后台补单/自动财务归因流程。

下单前先原子落盘意图。发单、终态、仓位、止损或最终落盘任一步不确定，保留 pending，拒绝该品种继续减仓、全平、反手、新开及自动改撤止损；启动、盘中巡检和日检均遵守，其他品种不因此永久停止。告警和页面异常标记持续提示人工。

**隔离不等于证明止损一定有效。** 请求前确认原止损，代码不会主动撤掉它；但交易所并发触发、改量失败及超量 reduce-only 止损的真实行为仍需实测。出现 pending 后必须人工核对，不能只等系统自己恢复。

人工核对顺序：记录操作号及当时账本 → 在 OKX 查本次普通订单终态/成交、现有仓位与全部止损 → 先确保实际残仓有有效保护 → 维护窗口内备份最新状态并据真实证据对账。证据不足不记假成交、不清 pending。禁止用旧备份覆盖新状态；pending 未解决时禁止回滚到不识别它的旧代码。当前不提供自动解除隔离按钮。

## 初始实现阶段的离线验证（历史检查点）

- Python 全量 **386 项通过**（四项修复检查点 359 项 + 减仓 27 项）。
- Node 前端单测 **4 项通过**：明确确认、取消/错误输入、快速双击、执行响应丢失。
- 覆盖多空、连续主动减仓、过期/篡改/重复预览、交易互斥锁、未授权/功能闸关闭、最小数量、0.1 面值换算、部分成交撤单、零成交、发单/改量超时、额外/错误止损、同时止损竞态、两阶段落盘失败、重启/日检隔离及剩余仓位策略语义。
- `ruff --select F,E9`、`compileall`、`node --check`、备份脚本 `bash -n`、`git diff --check` 通过。运行时海龟关键词扫描无命中。
- 本轮自审聚焦新交易指令、止损/持久化失败和旧策略/页面接入；没有把“测试全绿”计为三轮全量审查完成或两轮无问题部署门禁通过。

## 前端检查（使用 emil-design-eng 约束交互）

| Before | After | Why |
| --- | --- | --- |
| 单个“平仓”按钮 | 并列“减仓 / 全平”，待核对时均禁用 | 区分单次减仓与全部退出 |
| 无减仓预览 | 服务端计算数量，二次品种名确认 | 不让前端自行决定实际张数，减少误操作 |
| 仅统计逐条平仓记录 | 减仓历史单独标注，胜率按完整仓位合计 | 不因拆分退出虚增交易次数 |
| 按钮 transition: all | 限定属性，减少动态效果偏好与键盘焦点下不缩放 | 沿用风格，避免多余动画 |

内置浏览器此前明确报 `prompt() is not supported`，没有把该环境计为原生弹框通过。2026-09-06 19:33 改用真实 Safari，在仅监听 127.0.0.1 的离线交易所替身页面完成：点击减仓、取消不成交、50% 服务端预览、输入品种名确认、成功减仓、刷新后保留剩余仓位和 0.5% 原风险度、历史记录标为“减仓”。浮盈/名义金额/止损风险随 100 → 50 的测试仓位减半；求索指数 K 线仍渲染。页面 JS 使用原始候选文件，已移除临时诊断注入。测试标签页及本地预览进程已关闭；未在实盘网页点击交易按钮。

## 初始实现阶段的代码量与待办（历史检查点）

以下记录保留初始验收时的计数与待办；后续第三、第四轮已连续通过，以本文顶部终审记录为准。终审新增市价止损校验后，合并候选生产净增 378 行、测试净增 731 行。

- 仅减仓功能：生产代码净 **+370 行**；测试净 **+422 行**（Python 356 + 前端 64 + 既有测试夹具 2）。含空行/注释，文档及清单另计。
- 连同原四项修复相对 `bca3fb8`：生产净 **+375 行**，测试净 **+706 行**。没有依赖、调度、部署状态机或自动恢复链扩张。
- `TRADING_ENABLE_PARTIAL_CLOSE` 默认关闭；没有在任何服务器配置中启用。
- 50123 已解除；基础模拟盘减仓、conditional 原位改量、止损 ID/价格保持、剩余止损触发、两类响应丢失核对，以及改量未发送时旧止损超量保护，均取得上述实测证据。不得拿现有实盘仓位试验。
- Safari 原生确认流程已验收。当时的下一步为冻结候选并完成完整审查，386 项针对性回归不冒充全量审查。该待办现已由上文四轮主审完成；生产部署状态以最新发布记录为准。
