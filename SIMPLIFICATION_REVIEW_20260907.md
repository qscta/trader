# 2026-09-07 有界精简记录

基线：`cadd9f6d6cc1ab898a9aabe36016232be5c3d5cf`。状态：**代码提交 `31d2906aec4309076c2145dac21150f8c0b7a9f8` 已推送，并于 2026-09-07 13:19 部署；现场核验通过。** 下文分别记录精简验证和实际发布结果。

## 裁决

适合小幅去重，不适合大重构。判断标准是减少重复维护点、保留清楚的业务边界，而不是追求总行数最低。本次做到下面两项即停止，不扩大到交易安全流程。

| 修改 | 保持不变 | 生产代码净变化 |
| --- | --- | ---: |
| 止损重入复用现有 `check_current_state` 计算当前 EMA 方向 | 历史数量门槛、相等时不重入、固定 N 日收盘价止损、三元组及信号字段、输入 DataFrame 不变 | -23 行 |
| 五类权益文件共用一个原子保存及告警实现 | 文件路径、原子写入函数、布尔返回值、告警文本、异常传播、调用方的锁及保存顺序 | -12 行 |

不改公开方法签名，不新增状态字段、配置、依赖或自动恢复路径。前端、图表、下单适配器、减仓流程、账本事务、日检编排、止损保护和运维定义均未修改。上轮发现的 WLFI 旧全平成交价记账微差也不夹带修复。

## 未采纳的精简

- 不删交易所基类的接口声明：它们虽无具体实现，仍说明上层与适配器的职责，收益不足以支持仅按行数删除。
- 不把 mixin 改为组合：这会重排初始化和互相调用关系，目前没有足够收益覆盖风险。
- 不合并新交叉信号与当前方向判断：两者业务语义不同。
- 不合并不同阶段的订单确认、止损验证及隔离：重复检查未必冗余，外部状态可能在两次检查之间变化。
- 不泛化五类权益文件的读取与默认值：各类文件的数据结构及失败语义不同。本次只统一完全相同的保存政策。
- 不删测试来制造代码量下降，不把模拟盘脚本引入生产运行路径。

## 亲自执行的验证

- 修改前原有 389 项测试通过，真实 HTTP 被测试执行器禁止。
- 新增 6 项契约回归测试（含参数化子场景），在旧生产实现和新实现上均通过：覆盖多/空/相等方向、历史门槛、缺少止损数据，以及五类文件的真实写入、失败保留旧数据、精确告警、异常不被吞掉。
- 新版 395 项测试正序、逆序各完整运行一次：均通过，无跳过；4 项前端测试通过。
- 与固定基线的策略实现比较 1,030 组输入 × 3 个公开方法；含五组周期配置、随机行情、数据不足、相等及非有限值，输出一致且不修改输入。非有限输入仍受现有真钱行情校验阻断，差分试验不表示允许其交易。
- 两个修改类的原有公开方法签名一致；全部 Python 源码在内存编译通过；Ruff F/E9、前端语法、备份脚本语法及差异空白检查通过。
- 自有运行源码的海龟相关关键词扫描无命中；源码哈希清单同步本地候选。

这些是本次小范围重构的验证，不宣称重新完成三轮全系统终审，也不替代将来部署后的运行态检查。没有为本次纯内部去重再发送模拟盘或实盘订单。

## 代码量

按 Git 跟踪文件的物理行计，含注释和空行；自有 Python/HTML/JS/CJS，排除第三方 `static/lwc.js`，文档与清单另计。

- 生产及运维验证工具源码：7,893 → **7,858 行，净减 35 行**。
- 测试：6,275 → **6,368 行，净增 93 行**，即上述 6 项回归测试。
- 两者合计净增 58 行，增加来自不参与生产运行的验证代码；不能把它描述成整个仓库净减 35 行。

最初建议等待下一次必要发布合并上线。用户随后要求开始，本轮按既有维护门禁发布；不继续扩大精简范围。

## 现场发布结果

- GitHub 分支仍为 `codex/open-notification-severity-20260820`，非强制推送。11:38 前只完成推送，额度中断时尚未上传或替换生产文件；13:15 恢复后重新确认旧 PID、旧源码与原服务配置完整。
- 发布前再次运行 395 项离线测试，并核对服务器源码清单、38 个账本仓 / 38 个实仓 / 38 张市价止损一致，无 pending、孤儿仓或止损残留。
- 13:17:23 正常停止交易服务，先创建 root:root、0600 冷备 `/var/lib/trading-backups/pre-simplify-31d2906.tar.gz`（340145 字节），归档检查及同步成功。只替换两份运行源码、两份测试、本文和清单；全部应用 JSON 文件在复制前后哈希完全一致。
- 首次 PID `3316981` 带 `TRADING_DISABLE_NEW_OPENS=1`、`TRADING_ENABLE_PARTIAL_CLOSE=0`、`TRADING_SKIP_STARTUP_CATCHUP_ONCE=1` 启动，逐仓同步、调度与现场只读核验通过后再解除维护。
- 最终 13:19:15 启动 PID `3317723`，13:19:45 完成同步和调度。禁开仓开关已移除，减仓功能恢复为 1；本次进程的跳过补跑开关只用于把今天已完成的日检记入内存，不再重复触发开平仓。临时 drop-in 随后移出 systemd 并 daemon-reload，不重启此进程；未来重启不继承该临时开关，次日日检照常。
- 最终 38 仓 / 38 张唯一市价止损匹配；配置、账本、止损日期文件与冷备逐字节相同。48 个跟踪品种仍均为 0.5% 风险。OKX 原生历史查询确认 13:17:15 至 13:19:51 没有新增普通订单，也无待成交普通订单。
- 实际本地 HTTP 首页、静态 JS、状态和求索指数 OHLC 接口均为 200，前端原哈希不变、状态无异常。本次未另做公网已登录浏览器验收，不把本地 HTTP 核验说成公网端到端验证。
- 备份 timer 恢复 active，NEXT 为 **2026-09-08 07:50 CST**，未手动触发备份；资源监控与 Cloudflare 隧道仍 active，未重启。现场只读检查使用 SSH 标准输入，无检查脚本安装到生产目录。
