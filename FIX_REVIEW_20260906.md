# 2026-09-06 四项审查发现修复

基线：`bca3fb8`。状态：仅本地候选，未推送、未部署，未操作服务器或交易所。

本文件记录“四项修复完成、尚未添加减仓功能”的历史检查点；下文代码量和测试数仅适用于该检查点。后续按比例减仓候选的范围、验证及未完成门禁见 `PARTIAL_CLOSE_REVIEW_20260906.md`。

## 修复范围

| 发现 | 实施方式 | 生产代码净变化 |
| --- | --- | ---: |
| F1：反手平仓失败后遗忘反向方向 | 未确认平仓返回失败，日检不记完成；有仓日检按当前 EMA 方向处理，不再要求交叉必须发生在当天 | -4 行 |
| F2：空行情被当成日检完成 | 空响应、无已收盘数据、足量合格历史却未返回策略结果，均进入现有失败重试；非空但不足预热根数仍正常跳过 | +3 行 |
| F3：未送达异常告警被去重 | 前端异常状态与“已送达”内存记录分离；成功后去重，失败下轮重试，恢复时清除 | +5 行 |
| F4：求索指数压缩覆盖并发采样 | 扩大原有 RLock 范围，覆盖读取、分组和落盘，仍先保存历史 OHLC 再删除原始采样 | +1 行 |

合计：生产代码净 **+5 行**；测试代码净 **+284 行**，新增 **12 项测试**。按物理行统计，含注释及空行；文档与哈希清单另计。

## 保持不变的边界

- 每次平仓重试仍先核对交易所实际方向、数量；部分成交或人工加减仓导致不一致时隔离，不盲目重发。未确认平仓不撤原止损、不记平、不反手开仓。
- EMA 相等不反手；已删除/禁用品种只平不开；无仓首次开仓仍要求新交叉或既有重入标记。盘中巡检不新增开仓动作。
- 开仓总闸、交易所适配器、账本持久化、风险度计算、固定 N 日收盘价止损均未改；本次没有实现按比例平仓。
- 前端、求索指数展示算法、依赖、systemd、备份及其他运维文件未改。唯一新增内存表仅用于告警送达去重，不是交易状态，重启后重新核查并告警。

## 亲自执行的验证

- 修复前全量 347 项通过；修复后全量 **359 项通过**，两次运行均成功。
- 负对照：将新增测试放入从 `git archive HEAD` 导出的旧生产代码，四个针对性测试复现四类问题（含子场景共八处失败）；修复版通过相同覆盖。
- 新覆盖包括：同日平仓失败重试及成功去重、跨日无新交叉继续处理、双向 EMA/相等边界、部分成交隔离、删除品种只平不开、空行情恢复、告警失败/变化/恢复、真实双线程采样竞争、归档落盘失败保留原始采样。
- `ruff --select F,E9`、`compileall`、`node --check`、备份脚本 `bash -n`、`git diff --check` 均通过；运行时海龟相关扫描无命中。
- 所有测试均离线，使用临时账本及交易所/推送替身；没有实盘订单或真实钉钉消息。`SOURCE_MANIFEST.sha256` 已同步候选文件。

## 限制与下一步

本次是四项已复现缺陷的修复与回归验证，不宣称交易所极端行为已被代码测试证明，也不替代部署后的运行态核验。候选在本地维护仓库中；桌面其他副本及云服务器没有更新。部署需另行明确安排，不应把旧审查文档中的部署状态当成本候选已上线。
