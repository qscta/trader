# 代码审查说明（给 Codex）

## 背景
这是一套**真实资金**的程序化合约交易系统，原为币安版（海龟通道突破 + 双均线 EMA 策略，已实盘跑通）。
本次改动把它**收敛为「欧易（OKX）单所版」**：只对接欧易，删除币安与多交易所架构。

## 核心原则（本次改动的边界）
> 只改三类东西：①统计展示口径 ②交易所范围（删币安、单所化）③默认参数（双均线短周期 7）。
> **不改**已跑通的交易逻辑：交易信号、下单、止损创建/推进、平仓、以损定量的仓位计算、调度执行。
> 适配欧易若需改接口调用，**只能在交易所适配层（okx_api.py / exchange_base.py）做映射**，不得改变策略层的输入输出语义。

## 请重点核对的 5 点
1. **交易逻辑等价性（最重要）**：`main.py` 里的交易信号、以损定量、开仓后建止损、止损推进、平仓、平仓后状态记录、每日 08:00 调度，是否与币安原版**行为等价**，确认只是替换了适配层，没有动策略语义。
2. **OKX 适配正确性（真钱红线）**：`okx_api.py` 的——
   - 合约**张数 ↔ 币数量**换算（每张 = `contractSize` 个币），是否在下单边界内部正确换算、不外泄；
   - 止损算法单 `stopLossPrice` + `reduceOnly` 是否正确（reduce-only、触发市价平仓）；
   - `cancel_all_orders` 能否连算法止损一起撤掉；
   - `set_leverage` / 单向（净）持仓模式 / `tdMode` 是否正确。
3. **状态隔离与迁移**：从多所版的 `data/okx/*` **迁回项目根目录**的逻辑（`main.py` 的 `_migrate_okx_legacy_state`）是否安全——根目录已有状态时不得覆盖，避免串仓/丢仓。
4. **未创新高统计口径**：`equity_tracker.py` 的 `build_account_stats()`——
   - `days_since_peak`（未创新高天数）、`longest_drawdown_days`（历史最长未创新高）；
   - 当前正处于未创新高时，历史最长须 `max(历史已记录, 当前进行中天数)`；
   - 创新高当天 days_since_peak 归零，并用「旧峰值时间→本次新高」结算刚结束的那段更新历史最长；
   - 前端 / `/api/account_stats` / 钉钉周报是否**统一复用**这一份结果（周报不得自己手算）。
5. **单所收敛是否彻底**：是否还有币安 / 多交易所残留（`exchange` 参数、`/api/overview` 等多所路由、前端交易所切换/汇总页）。

## 文件清单与角色
| 文件 | 角色 | 本次是否改动 |
|---|---|---|
| `main.py` | 主程序：装配、交易调度、信号/开仓/止损/平仓/周报 | **改**（装配层 + 周报复用 + 统计调用；策略逻辑应保持等价） |
| `okx_api.py` | 欧易适配器（张数/止损/杠杆/符号映射） | 适配层 |
| `exchange_base.py` | 适配基类（`filter_closed_candles` 等共用） | 适配层 |
| `equity_tracker.py` | 权益/峰值/求索指数/统计口径 | **改**（未创新高口径） |
| `api_server.py` | Flask HTTP API | **改**（去多所、单所路由） |
| `wsgi.py` | gunicorn 入口 + 单实例文件锁 | **改**（单所启动） |
| `ma_cross_strategy.py` | 双均线策略 | **改**（默认短周期 7/长 28） |
| `turtle_strategy.py` | 海龟通道策略 | 未改（上下文） |
| `risk_manager.py` | 以损定量风控 | 未改（上下文） |
| `trade_state.py` | 本地持仓/止损/信号状态 | 未改（上下文） |
| `dingtalk_notifier.py` | 钉钉通知 | 未改（上下文） |
| `verify_okx.py` | 欧易模拟盘验证脚本（开/平/止损全链路） | 新增 |
| `config.example.json` | 单所配置样例 | **改** |
| `index.html` / `static/app.js` | 前端（单所） | **改**（去切换/总览） |
| `tests/test_trading_logic_unittest.py` | 交易逻辑/路由单测 | **改**（适配单所） |
| `test_equity_drawdown.py` | 未创新高统计三场景单测 | 新增 |
| `test_daily_summary_delivery.py` / `test_trade_state_fees.py` / `test_turtle_strategy_regression.py` | 既有单测 | 既有 |

## ⚠️ 红线尚未验证
OKX 的**张数换算 / 止损算法单 / 杠杆与单向模式**目前**只过了代码与单测，尚未在欧易模拟盘实测**。
上实盘前必须用 `python verify_okx.py`（配置 `sandbox: true`）跑通开/平/止损全链路再放大资金——代码审查替代不了这一步。

## 本机测试现状
无需第三方依赖即可跑、且已全绿（32 用例，同进程任意顺序验证无桩污染）：
`test_equity_drawdown`(3) / `test_daily_summary_delivery`(7) / `test_symbol_removal_management`(11) /
`test_trade_state_fees`(3) / `test_turtle_strategy_regression`(8)。
`tests/test_trading_logic_unittest.py`（含 DeleteSymbolApiTests）需 pandas/ccxt/flask 环境运行。

> 注：本包不含真实 `config.json` 与 `data/` 状态（含密钥与持仓），仅含 `config.example.json` 样例。

---

## 对上一轮 11 条审查意见的逐条回应（复审请重点核对）

1. **删除语义**：维持「移出品种池，不平仓不停托管」。已在前端确认文案、README 明确。
2. **删除不动持仓/挂单**：`api_server.delete_symbol` 只改 `config.trading.symbols`，不碰 `trade_state`、不撤单、不平仓（既有行为，未改）。
3. **已删除但有持仓仍检查**：`check_and_execute_trades()` 一直有 `symbols_to_check.update(all_open_positions.keys())`（既有行为，未改），本轮补了单测锁定。
4. **按持仓 strategy 托管**：配置缺失时用 `position['strategy']` 构造 symbol_config（既有行为，未改），本轮补了单测锁定。
5. **缺 strategy 兜底（新修）**：`delete_symbol` 删除前检查持仓——缺 `strategy` 时先从当前配置补写进持仓（`trade_state.set_position_strategy`，新增方法）；配置也没有则 400 拒删并提示。不会静默默认成海龟。
6. **前端文案（新修）**：删除确认改为「删除后将从品种池移除，不再新开仓；如已有持仓，系统会继续按持仓记录的策略跟踪、更新止损并等待平仓。」（static/app.js）
7. **单测（新增）**：`test_symbol_removal_management.py` 9 用例——已删除品种仍进检查集合且按持仓 ma_cross 托管 / 池内+孤儿并存各按各策略 / 缺 strategy 兜底 turtle / 状态归属护栏 4 例 / set_position_strategy 持久化 2 例。另在 `tests/test_trading_logic_unittest.py` 加 `DeleteSymbolApiTests` 4 用例（删除不平仓不撤单、补写策略、拒删）。
8. **状态目录串仓（新修，红线）**：`main._guard_state_owner()` 启动校验 `trade_state.json` 顶层 `exchange` 归属标记：标记 okx 放行；标记其它所**拒绝启动**；无标记且空仓自动认领；**无标记但有持仓拒绝启动**并给出人工处置指引（加标记或独立目录），绝不静默把旧币安状态当欧易复用。`data/okx/` 自动迁移时顺带打 `okx` 标记。
9. **OKX 模拟盘验证**：保持共识——`verify_okx.py` 全链路（开/建止损/撤止损/触发/平仓无残留），上线前必跑，代码审查不替代。
10. **测试污染（新修）**：废弃 `sys.modules.setdefault` 常驻桩，统一 `_test_stubs.import_main()`——桩只在导入 main 的瞬间存在，导入完立即恢复 `sys.modules` 并弹出绑定桩的 main 缓存。已验证 30 用例同进程正序/倒序/discover 三种方式全绿。
11. 谢谢确认；统计口径、EMA 6/28、单所收敛维持不变。


---

## 全量自审一轮（Fable 5）：新增修复清单

本轮按"只动适配层与明确 bug，不碰策略语义"的红线，从头通读全部源码后修复：

1. **OkxApi.get_position 归一化（高）**：OKX 可能返回 contracts=None/0 的空仓条目；原样外泄会让上层
   （币安时代写下的）`contracts == 0` 判断因 None 失效，盘中巡检 `contracts > 0` 更会直接 TypeError
   （每 5 分钟中断一次全品种巡检）。现无实仓统一返回 None（符合基类契约）。
2. **_fetch_algo_orders 不再吞异常（中）**：原先三种参数变体全失败时静默返回 []，导致 cancel_all_orders
   误报"无算法单需要撤销"——上层以为止损撤干净，实际可能残留活止损单。现全部失败时向上抛出，
   调用方既有的降级路径（algo_ok=False / 重试确认）即可生效。
3. **check_and_execute_trades 单品种异常隔离（中）**：原先整轮共用一个 try，任一品种异常会中断当轮
   其余品种的止损推进/平仓检查。现 per-symbol try/except；有失败品种时当日不标记完成，
   由 +1 分钟重试调度整轮重跑（开仓有持仓检查、止损更新有同价跳过、平仓有交易所端张数上限，幂等安全）。
4. **instant_open / close_position 路由加交易锁（中）**：原先与 08:00 日检、盘中巡检无互斥，
   存在并发双开/双平窗口。现非阻塞获取 system._trade_lock，拿不到返回 409。
5. **清理**：equity_tracker 移除上轮口径重构后遗留的死字段 current_drawdown_start；
   api_server 过时注释更正 + Flask>=2.2 的 ensure_ascii 兼容写法。

新增测试：PerSymbolIsolationTest 2 用例（单品种异常不阻断其余品种 / 全成功才标记当日完成）。
本机 32 用例全绿（discover 与倒序双跑）。

**复审重点**：上述 1/2 在 okx_api.py，3 在 main.py check_and_execute_trades，4 在 api_server.py。
请确认 3 的重跑幂等论证成立，以及 1 不影响你环境中 ccxt fetch_positions 的返回结构假设。

---

## 简化收尾（死代码清除 + 工具去重）

多所收敛后的残留清理，全部为零行为变更或等价改写：
- 删除死代码：api_server 的 atomic_write_json / notify_persistence_failure 一套 / _manual_check_locks（均无调用方）；exchange_base 的 get_positions / fetch_open_orders（上层无调用）。
- 工具去重：atomic_write_json 三份重复实现收敛为 trade_state 一份，equity_tracker / main 改为 import；main._save_stop_loss_dates 的手写原子写改用同一实现。
- 单所化收尾：manual_check 防重入从按交易所字典改为模块级布尔（_set_manual_check_running）。
- 验证：32 用例 discover + 倒序全绿。


---

## 对第四轮 3 条交易安全意见的逐条回应

1. **contractSize fail closed（已修）**：`_get_contract_size` 获取失败或字段缺失时抛 `ContractSizeUnavailable`，
   不再默认 1.0；换算入口 `_coin_to_contracts` / `round_quantity` 向上传播，开仓/平仓/止损换算一律放弃本次交易。
   日检场景由 per-symbol 隔离捕获并进失败品种钉钉汇总；即时开仓路由返回 500 与错误信息。
2. **撤算法止损可验证（已修）**：`_cancel_algo_order` 改为验证式——尝试所有取消参数组合后，
   **以「算法单列表查无此 id」为成功标准**；OrderNotFound 不再直接当成功（某参数组合把 id 当普通单查也报它）。
   `cancel_order` 的普通单 OrderNotFound 分支同样改为列表裁决。
3. **撤不干净则阻断（已修）**：新增 `main._cancel_stop_order_confirmed` 统一封装四处调用
   （止损更新 / 信号平仓 / 双均线翻转 / 开仓回滚）+ api_server 手动平仓路由。不可确认时：
   - `trade_state` 持久化「止损残留」标记（重启不丢），严重告警；
   - 止损更新：**不创建新止损**，本地保持旧止损记录（与交易所现存挂单对应）；
   - 翻转：平仓记账完成但**不反手开新仓**；
   - `_execute_open` 开头检查残留标记，**阻断该品种一切新开仓**；
   - 每日检查前 `_retry_clear_stop_residues` 自动重试清理（只对已无持仓品种盲撤，有持仓品种的"残留"
     即本地记录的现行止损，交由止损更新流程处理），确认清理后解除阻断并通知恢复。
4. **止损创建超时确认严格匹配（已修）**：`_algo_order_matches` 要求 方向+触发价+张数 全部吻合
   （触发价兼容 stopLossPrice/triggerPrice/info.slTriggerPx 等字段，相对容差 1e-6；任何字段读不到一律不匹配，
   宁可重试创建也不误认残留旧单）。
5. **单测（已补）**：本机新增 `test_okx_adapter_safety.py` 14 用例（面值 fail-closed 4 / 验证式撤单 4 /
   严格匹配 6，桩 ccxt 可本机跑）+ 残留阻断 2 用例（含持久化重启）。`tests/test_trading_logic_unittest.py`
   新增：撤旧失败不更新止损、平仓撤失败仍记账并标记、翻转撤失败不反手；并把既有断言对齐现行缓冲通知行为
   （此前部分断言仍按旧的直发通知写，在依赖环境本来就会挂）。
   本机 48 用例 discover + 倒序全绿。


---

## 对第五轮 2 条 P2 意见的回应

1. **平仓后撤止损失败即终止（已修）**：`handle_close_signal` 在平仓记账完成后，若 `stop_cleared == False`
   立即 `return False`——本函数不进再开仓分支，调用方（海龟反手）拿到 False 也不开新仓；
   不再依赖 `_execute_open` 的残留检查做下游兜底（兜底仍保留作多一道防线）。
2. **迁移边界保护（已修）**：`_migrate_okx_legacy_state` 改为——根目录有 `trade_state.json` 时：
   旧文件无持仓→不动；旧文件有持仓而根文件**空仓**→备份空文件（`.bak.empty.时间戳`）后迁移旧持仓；
   **两边都有持仓→拒绝启动**等人工；任一文件读取失败→拒绝启动（持仓不明不得继续）。
   新增 4 用例（备份后迁移+打标 / 双持仓拒启 / 旧空不动 / 损坏拒启）。

本机 52 用例 discover + 倒序全绿。模拟盘全链路验证（开仓、止损创建/更新、平仓、反手、删除后托管）仍为上实盘前置条件。


---

## 对第六轮 1 条 P1 + 2 条 P2 的回应

1. **P1（已修）「交易所无仓、本地有仓」补偿路径确认止损消失**：四条路径
   （启动同步 `sync_positions_on_startup` / 盘中巡检 `reconcile_intraday_stop_losses` /
   海龟与双均线的止损确认分支）在记平本地仓后，统一调用 `_cancel_stop_order_confirmed`：
   - 若旧止损因触发而消失 → 列表验证通过，顺带 clear 残留标记；
   - 若是手动/外部平仓留下的活残留单 → 验证式撤销真撤掉它；
   - 撤销不可确认 → 标记残留+告警；**海龟止损分支额外显式 return，不进入止损后反手**（不依赖下游兜底），
     双均线 T+1 重入与巡检后开仓由 `_execute_open` 残留阻断挡住。
2. **P2（已修）`_fetch_algo_orders` 合并查询**：三种参数组合全部尝试、按订单 id 合并去重；
   只有全部失败才抛。消除「某组合成功但返回空 → 误判无算法单」。
3. **P2（已修）普通撤单 fallback 复验**：`cancel_order` 走普通撤单成功后，仍以
   `_algo_order_absent` 复验算法单列表，复验不过不返回 True。

新增 11 用例：盘中巡检撤单确认 2（本机，真 TradeState）/ 海龟止损分支撤失败不反手 1（tests）/
合并查询 3 + fallback 复验 2（本机，桩 ccxt）/ 既有 fake 补齐若干。本机 59 用例 discover + 倒序全绿。


---

## 全维度深审第二轮（Fable 5 终审）

通读了此前从未深审的部分：dingtalk_notifier（干净）、turtle_strategy 全文（逻辑严密，8 回归测试锁定，不动）、
前端 app.js 全文 + index.html（JS 引用的全部 DOM id 核对存在、登录回车绑定在位）。新修 3 处：

1. **写接口输入校验（api_server）**：`/api/symbols`(POST/PUT)、`/api/instant_open` 此前对输入零校验。
   现统一 `_validate_symbol_input`：symbol 必须 `^[A-Z0-9]{1,20}USDT$`（挡 HTML/特殊字符进 config 与前端渲染）、
   risk_per_trade ∈ (0, 50%]（防"把 1 当 1% 输"的数量级笔误直接放大仓位）、strategy 白名单 turtle/ma_cross。
2. **残留自动清理护栏（自审发现）**：`_retry_clear_stop_residues` 原以「本地无仓」为盲撤前提——
   若用户在欧易**手动开仓+手动挂止损**（本地无记录），同品种曾标记残留时盲撤会把手动止损一并撤掉。
   现增加交易所端持仓检查：交易所有仓（疑似人工仓位）即跳过并告警，残留标记保留继续阻断。
3. **日志路径锚定**：`trading.log`（main 的 RotatingFileHandler 与 /api/logs 读取）从相对路径改为
   锚定项目目录的绝对路径，避免 systemd/cron 等不同 cwd 启动时日志写错位置/读不到。

新增 8 用例（输入校验 6 / 清理护栏 2）。本机 61 用例 discover + 倒序全绿。


---

## 对第七轮（完整依赖环境实测）清单的回应

1. **MaCrossTPlusOneTests 桩过期（已修）**：fake system 补 trade_state.get_open_position / notifier，
   适配 handle_no_position_ma_cross 开仓后的持仓确认逻辑。
2. **TradeState 事务化（已修，采纳并升级）**：add_open_position / update_stop_loss / close_position
   统一改为「快照→修改→落盘，失败回滚内存再抛」（新增 _save_or_rollback_locked）。
   比清单建议的"仅 add 回滚"更进一步：三个方法全部事务化——因为 force_runtime_* 补偿通道本就存在，
   调用方 fallback（_update_trade_state_stop_with_runtime_fallback / _close_trade_state_with_runtime_fallback）
   在捕获异常后调用 force_runtime_* 强制内存反映交易所现实。底层事务 + 上层显式补偿，语义自洽：
   - 开仓保存失败：内存回滚（交易所侧已撤单平仓）→ 无假仓，前端不显示幽灵持仓，巡检不会再记假平仓；
   - 止损更新保存失败：内存回滚 → fallback 强制写入新止损（交易所新止损已挂）+ 告警；
   - 平仓保存失败：内存回滚 → fallback 强制移除持仓（交易所已平）+ 告警。
   原有的 TradeStatePersistenceFailureTests 三个测试（期望回滚）现在与实现一致。
3. **有持仓禁改策略（已修，轻量版）**：PUT /api/symbols/<symbol> 在该品种有持仓时拒绝修改 strategy
   （400，提示平仓后再改或删除品种让持仓按原策略托管）。未按"长期规则"改主循环的策略优先级——
   那会动核心检查逻辑（红线），拒绝入口达到同等安全效果。
4. **strategy_params 范围校验（已修）**：周期 ∈ [2,500] 整数；ma_short_period 必须 < ma_long_period
   （按提交值+现有值的合成视图判定）；default_risk_per_trade ∈ (0, 50%]；非法 400、不写配置、不 reload。

新增测试：本机 TradeStateTransactionTest 4 用例（三方法回滚 + force_runtime 通道不受影响）；
tests/ 补 有仓拒改策略 / short>=long 拒绝 / 风险度越界 3 用例。本机 65 用例 discover + 倒序全绿。
请在完整依赖环境复跑 tests.test_trading_logic_unittest 确认全绿。


---

## 对第八轮 1 条交易安全边界的回应

**「交易所无仓」分支落盘失败时撤单确认被跳过（已修）**：海龟/双均线的止损确认分支原先在
`state_saved=False`（落盘失败、已走 runtime 补偿记平）时提前 return，跳过 `_cancel_stop_order_confirmed`——
若欧易实际无仓但旧止损条件单仍挂着、本地保存又恰好失败，系统既不确认清理也不标记残留。
现改为：**只要 closed_position 拿到（记平成功）就先执行撤旧止损确认**；`state_saved=False` 时在确认后
再 return——海龟不反手、双均线不记 T+1，与原有补偿语义一致。
（核对确认：盘中巡检与启动同步两处的撤单确认本就在 state_saved 判断之前，无此问题。）

新增本机单测 StopConfirmOnPersistFailureTest 2 用例：构造 close_position 抛 TradeStatePersistenceError、
force_runtime 补偿成功的场景，断言两分支的撤旧止损确认必然执行、且不反手/不重入/不记 T+1。
本机 67 用例 discover + 倒序全绿。


---

## 终轮主动增强（Fable 5 提出，非审查意见驱动）

**止损自愈巡检**：此前体系对「撤止损」有验证式确认与残留阻断，但对「止损存在」本身没有守护——
建新止损失败、人工在交易所误撤、交易所端丢单等都会让仓位裸奔，只有告警等人工。
现盘中巡检在「本地与交易所都有仓」时顺带校验止损存在性（`_ensure_stop_order_alive`）：
- 健在判定（第九轮已升级为三态严格匹配）：方向+触发价+张数全部一致才算 intact；
  id 在但内容不符为 mismatch（告警人工）；其余为 missing（补挂）；
- 丢失则按本地 stop_loss_price 补挂，成功后同步本地 stop_order_id 并钉钉通知；
- fail-safe：算法单查询失败 / 品种处于残留阻断 / 补挂失败，一律不动或仅告警，绝不加剧状态混乱。

**残留阻断可观测**：/api/status 返回 stop_residues；前端顶栏常驻红色警示 pill（含品种列表与说明 tooltip），
不再只靠一条钉钉。已用预览实测渲染。

新增本机 StopSelfHealTest 6 用例（id 健在 / 价格匹配健在 / 丢失补挂+同步id+通知 / 补挂失败告警 /
残留品种跳过 / 查询失败跳过）。本机 73 用例 discover + 倒序全绿。

**模拟盘验证清单新增一项**：人工在欧易撤掉止损单 → 等一个巡检周期 → 确认自动补挂 + 钉钉通知。


---

## 对第九轮 1 条 P2（止损自愈匹配口径）的回应

**采纳并下沉适配层（已修）**：自愈判定改为三态，由新增的 `OkxApi.find_stop_order_state` 完成
（张数换算留在适配层内部，维持「张数不外泄」分层）：
- `intact`：列表中存在 方向+触发价+张数 与本地记录**严格一致**的算法单（复用 `_algo_order_matches`）——
  消除「同方向同触发价但张数不足的人工单被误认为保护完整」的部分裸奔风险；
- `mismatch`：本地记录的 stop_order_id 还在列表里，但内容与本地不符（疑被人工改挂）——
  **告警人工、不自动补挂**（盲目补挂会造成双止损，盲目视为健在会裸奔，这是第四条 fail-safe）；
- `missing`：补挂（原逻辑）。同价但张数不足的人工单场景判 missing → 补挂全额 reduce-only 止损，
  即使与人工半张单并存，reduce-only 语义保证不会超平，保护方向正确。

新增测试：okx 侧 FindStopOrderStateTest 5 用例（严格一致 intact / 同价异量不算 intact /
id 在内容不符 mismatch / 空列表 missing / 面值失败传播）；主层 StopSelfHealTest 重写为三态桩并新增
mismatch 告警场景。本机 78 用例 discover + 倒序全绿。


---

## 对第十轮 2 条 P3 的回应

1. **文档口径同步（已修）**：main._ensure_stop_order_alive 的 docstring 与审查说明中的旧描述
   （"方向+触发价"）已更新为三态严格匹配口径。
2. **mismatch 前端可见性（已修，并附赠告警节流）**：新增 `self._stop_anomalies` 内存状态
   （mismatch / replant_failed 两类；无需持久化——巡检每 5 分钟重新判定会自动重建/清除）：
   - /api/status 返回 `stop_anomalies`，前端顶栏新增独立红色警示 pill「止损异常待人工」（已预览实测）；
   - 顺带修了一个清单未点破的问题：mismatch/补挂失败在巡检每轮重复判定下会**每 5 分钟发一次钉钉**——
     现在告警只在状态首次进入时发，状态恢复（intact/补挂成功）自动清除警示。

新增/更新断言：mismatch 二轮巡检只告警一次且记入异常、intact 清除异常、补挂失败节流。
本机 79 用例 discover + 倒序全绿。


---

## 对第十二轮 1 条 P3（异常警示生命周期）的回应

**采纳主体、不采纳一个子项**：
- **已修**：仓位结束时统一清除 `_stop_anomalies`——清理点收口在 `_close_trade_state_with_runtime_fallback`
  （信号平仓/止损确认/翻转/巡检记平/启动同步全部经过它，含落盘失败的 runtime 补偿分支），
  api_server 手动平仓路由两个分支同步清理。异常单实体仍由调用方的撤单确认链路负责撤掉。
- **不采纳「删除交易对时清除」**：删除的语义是「持仓继续托管」——仓还在、异常止损单还在，
  警示依然真实，删除时清掉反而灭掉活警报；无持仓品种不会产生异常状态，不存在泄漏路径。

全面复审顺手修正：`_close_trade_state_with_runtime_fallback` 元组解包写法拆为两行（可读性）；
`update_strategy_params` 无 JSON body 时返回 400 而非 500。
复核 `_stop_anomalies` 全部 12 个触点的生命周期闭环（初始化/置位+节流/三类清除/展示）。
本机 80 用例 discover + 倒序全绿。


---

## 对第十三轮 1 条 P3（接口健壮性）的回应

**采纳并扩大修复面**：不止 update_symbol 一处——全部写路由的 `request.json` 统一换成
`request.get_json(silent=True)`（新版 Flask 下 `request.json` 无 body 会直接抛异常，
`if not data` 都来不及判断，被外层 except 转成 500）。update_symbol 补空 body 400 防护，
与 update_strategy_params / add_symbol / instant_open / close_position 口径一致。
tests/ 补 2 个空 body 用例（update_symbol / strategy_params）。


---

## 终审包说明（全项目版）

本包为**全项目终审版**（30 个文件），相比此前的核心审查包新增：
- `static/lwc.js`：TradingView lightweight-charts 官方库本体（minified 第三方代码，无需审查，
  可与 jsdelivr 发布版比对完整性）；
- `requirements.txt`：部署依赖清单（本轮新增）；
- `test_startup_smoke.py`：启动装配链冒烟测试 5 用例（本轮新增——此前全部测试用 __new__ 绕过构造，
  __init__ 端到端从未被测过：现覆盖 配置加载/旧格式拍平/凭据缺失拒启/状态迁移/归属护栏/部件装配/
  任务注册/示例配置可启动）；
- `tests/README.md`：更新为当前测试矩阵说明（顺带移除了旧版中的真实服务器路径）。

**脱敏声明**：打包前对全部文件做了程序化扫描（硬编码凭据 / 钉钉 webhook token / 私钥 / 长 hex 串
四类模式），全部通过；包内不含 config.json、trade_state.json、data/、日志与任何历史备份文件。

**终审验证建议**：`pip install -r requirements.txt` 后——
1. `python -m unittest discover -s . -p "test_*.py"`（85 用例，无第三方依赖也可跑）
2. `python -m unittest tests.test_trading_logic_unittest`（50 用例）
3. 静态：py_compile 全文件 / node --check static/app.js / config.example.json 校验


---

## 终极审判轮（Fable 5 收官，非审查意见驱动）

四个此前从未触达的验证维度（test_final_judgment.py，10 用例）+ 一项实质防护增强：

1. **孤儿仓告警（新防护）**：本地状态彻底损坏丢失（主文件+备份全毁）时，系统会当全新状态启动，
   而交易所上的真实持仓将变成无人托管的孤儿（不推止损、不检查平仓、前端不显示）。
   `sync_positions_on_startup` 此前只查「本地有、交易所无」——现增加反向核对
   （新增适配层 `list_position_symbols`）：交易所有仓但本地无记录 → critical 告警人工确认，
   不自动接管（可能是人工仓位）。fail-safe：核对失败只 warning 不阻断启动。
2. **并发混沌**：8 线程 × 30 组完整生命周期（开/改止损/平/残留标记）并发压榨真 TradeState——
   零异常逃逸、终态计数自洽、磁盘 JSON 与内存一致。
3. **灾难恢复**：主状态文件写入垃圾 → 自动从 .bak 恢复持仓（该恢复路径首次被测试覆盖）。
4. **时间边界**：求索指数 08:00 切日整点归属、跨年 00:30 归前一交易日、闰日、tick 分桶对齐。
5. **风控数学性质**：固定种子随机 2000 组（权益 100~100万、价格 0.00001~100万、风险 0.1%~50%），
   证明「以损定量」的不变量：实际风险绝不实质性超出预算（容差仅为 round 步长损失）。
6. 变异测试 7/7：面值 fail-closed / 验证式撤单 / 张数匹配 / 归属护栏 / 事务回滚 / 残留阻断 /
   海龟中轨判定——逐个故意改坏，测试矩阵全部立即变红（防线非装饰品的证明）。
7. ResourceWarning 清零（-W error::ResourceWarning 下全绿）；锁序证明 T→C→S 偏序无环、E 为叶子锁。

本机 95 用例 discover + 倒序全绿。


---

## 用户确认轮（策略逻辑核对 + 参数/前端调整 + 四修复两加固）

与用户逐条核对海龟/双均线策略逻辑（通道口径、以损定量、交易所侧止损、08:00 调度、T+1、
反手、止损棘轮、中轨资格门槛均确认一致）后，经用户逐项批准执行：

**参数与前端**：
1. 双均线默认短周期 6 → 7（ma_cross_strategy 类默认、main/api_server 兜底、config.json 与
   config.example.json、README 共 8 处）。
2. 前端两处策略下拉框默认改为双均线（index.html option 顺序，后端 API 缺省仍为 turtle——
   前端总是显式传值，改后端缺省会静默改变老脚本行为；持仓/配置缺字段的 turtle 兜底属老数据
   解释口径，不动）。
3. 新增暗色主题（不替换亮色）：顶栏 🌙/☀️ 手动切换 + localStorage 记忆 + 首帧内联脚本防闪白，
   整套 CSS 令牌覆盖 + K 线图配色联动（chartThemeOptions / applyTheme）。预览实测两主题渲染
   与刷新记忆正常。

**修复（上两轮审查发现，用户批准）**：
4. 手动检查不再标记当日完成（main.check_and_execute_trades `elif not manual_run`）——
   否则 00:00–08:00 间手动触发会吃掉当天 08:00 正式日检。
5. longest_drawdown_days 结算下沉到 reconcile_peak_equity（新增 _settle_drawdown_streak）——
   修复「5 分钟采样抢先落盘新峰值 → 统计刷新看不到刚创新高 → 历史最长漏记」。
   build_account_stats 原结算块保留供 persist=False 展示。
6. 双均线翻转撤旧止损不可确认时记录 T+1（_flip_position）——残留清理确认后次日按 EMA 方向
   自动重入，恢复「永远在市」（此前会一直空到下次交叉，且旧日志文案错误声称日检会重开）。
7. api_server.send_dingtalk 统一走 DingTalkNotifier——修复 errcode 不校验（被关键词拦截仍记
   成功）与 手动平仓/参数更新/资金同步 消息缺「交易」关键词被静默拒收；操作通知变为 markdown。

**撤销确认机制加固（用户授权主动加固）**：
8. `_cancel_algo_order` 验证复查：首次复核仍见该单时等待 CANCEL_VERIFY_RECHECK_DELAY(2s)
   复查一次再裁决——交易所列表滞后于撤单生效时不再误报残留（误报会白白阻断开仓一整天）。
9. `_update_stop_order` 挂新止损前复核交易所持仓仍在：撤旧确认瞬间止损恰好已触发/人工平仓时
   不再挂出无仓对应的孤儿 reduce-only 条件单（fail 方向：查询失败按持仓仍在继续挂新，
   保护现有仓位优先）。本地仓位交由巡检/日检记平。

新增测试 9：manual_run 不标记当日 1 / 采样先行结算时序 1 / 翻转 T+1 本机 2 + tests/ 断言更新 2 /
验证复查 2 / 挂新前复核 3。本机 104 用例 discover 全绿。tests/test_trading_logic_unittest.py
（依赖版）本机无 flask/pandas/ccxt 未跑，MaCrossFlipTests 两处断言已对齐新行为，请在完整环境复跑确认。
（后记：已在临时 venv 安装依赖复跑，50 用例全绿。）


---

## 稳定性与架构打磨轮（Fable 5，用户命题「像艺术品」）

全部为零策略语义变更的稳定性加固 + 一项 fail-closed 账本语义变更（Codex 意见，采纳并加重论据）：

**账本 fail-closed（语义变更，Codex 第 N 轮意见采纳）**：
1. `TradeState.load_state()` 改为：主文件不存在→全新启动（残留孤立 .bak 时日志提示人工）；
   主损坏备可读→恢复；**主备全毁→抛 TradeStatePersistenceError 拒绝启动**。
   加重论据：失忆运行不只是旧仓无人托管——日检会把有真实仓位的品种当空仓**重复开仓**
   （单向模式下同向叠加敞口/反向误减仓），主动误操作比被动漏管更危险。
   main.__init__ 捕获后先发钉钉严重告警再抛出（避免进程静默退出无人知晓）。
   孤儿仓告警语义收窄为「账本丢失（文件不存在）/人工仓」场景，只告警不拒启。
   灾难恢复测试组重写：备份恢复 / 主备全毁拒启 / 无备份拒启 / 全新启动 / 误删主文件不复活备份，5 场景。

**崩溃与环境稳定性（零语义变更）**：
2. `atomic_write_json` 写后 flush+fsync 再 rename——掉电不再可能留下空/半截状态文件。
3. 全项目文件 IO 显式 `encoding='utf-8'`（trade_state/equity_tracker/main/api_server/verify_okx/
   mem_monitor）——systemd 等 C locale 环境下读写含中文的 config/状态不再 UnicodeError。
4. trade_state 的 print 全部换 logger（状态损坏/备份恢复是最该进日志与告警链路的事件）。
5. equity_tracker 五个 load_* 不再静默吞损坏（except: pass → warning 带文件名），
   峰值/历史被悄悄重置的场景首次可观测。

**并发与隔离（零语义变更）**：
6. 盘中巡检循环体提取为 `_reconcile_symbol_intraday` + per-symbol try——止损自愈补挂
   （面值不可得）或撤单确认（重试耗尽 raise）单品种异常不再中断其余品种巡检（与日检同标准）。
7. `send_daily_position_summary_if_due` 加 `_summary_lock`——独立兜底调度与日检结束调用
   并发时的双发窗口关闭。
8. wsgi 单实例锁文件按项目目录 sha1 派生——同机多套独立目录部署不再互相拒启，
   同目录多 worker 互斥不变。

**确定性与整洁（零语义变更）**：
9. 日检/巡检遍历 sorted()——日志顺序跨轮可对比。
10. get_logs 参数 clamp [1,1000] + errors='replace'；海龟信号回填段空行噪音清理（语句逐条未动）。

新增/重写测试：灾难恢复 5（含 3 个 fail-closed 新场景）/ 盘中巡检单品种隔离 1；
test_daily_summary_delivery 桩补 _summary_lock。
本机 109 用例 + 依赖版 50 用例（venv）全绿。


---

## 架构收官轮（用户批准全量执行路线图）

以 git 提交为单位（项目自此纳入版本控制，.gitignore 排除凭据/状态/日志）：

1. **git init + 基线提交**——「像艺术品」的第一要素是可回退。
2. **启动补跑（行为变更，用户批准）**：`_run_startup_catchup_check`——已过今日检查时间且
   今日未跑（宕机/重启跨越 08:00）则启动即补跑一轮；补跑与正点等价（已收盘日线+幂等防护），
   调度窗口（+2 分钟缓冲）内启动让正常 cron 先走。新增 4 用例。
3. **api_server 配置事务去重**：五处「持久化失败→回滚→500」样板收敛为
   `_commit_config_or_rollback`（响应文案与回滚粒度逐字保持）。
4. **「交易所已平」统一收尾**：启动同步/盘中巡检/海龟日检/双均线日检四处重复的
   「记平→撤旧止损确认」前缀收敛为 `_handle_exchange_flat_close`（顺序与各分支后续语义不变）。
5. **只读统计 5 秒缓存**（build_account_stats persist=False 短缓存，persist=True 完整执行并刷新缓存）
   + **钉钉发送失败 1s 重试一次**（告警不再因网络抖动丢失）。
6. **架构拆分第一阶段（mixin）**：止损防线拆出 `stop_guardian.py`（StopGuardianMixin），
   通知报表拆出 `reporting.py`（ReportingMixin）；方法逐字平移、仍绑定 TradingSystem 实例
   （self 语义/测试桩打法/日志零变化）。main.py 1600→1223 行，保留装配/迁移护栏/调度/
   两策略信号执行（红线核心刻意集中一处便于审查）。
   **第二阶段**（信号处理与开平仓执行拆分）明确压后至模拟盘全链路验证通过之后。

全程每步提交前后跑双套件：本机 113 用例 + 依赖版 50 用例 + 启动冒烟，全绿。


---

## 尾账清算轮（历史遗留 5 小项 + Codex「误删主账本」P1）

对全部历史审查意见做逐条对账后，清掉此前标记为 P4/优化项但一直未修的 5 个尾巴，
并采纳 Codex 新一轮 P1（选「拒绝启动」方案而非「自动恢复」）：

1. **Codex P1（已修，选更严方案）**：主账本 trade_state.json 不存在但 .bak 仍在（疑似误删）
   → 拒绝启动。不自动恢复：.bak 是上次保存前的副本，可能落后于被删主文件，静默复活等于
   凭空捏造持仓；也不空启动（失忆）。报错信息给出人工二选一指引（cp 恢复 / 删 .bak 确认重置）。
   全新部署（两个文件都不存在）不受影响。测试改写 + 新增「删两份方可重置」用例。
2. **超时确认返回币数（契约修复）**：okx open_position / close_position 超时后经持仓查询
   确认的返回单，amount 原为张数差（张数外泄，当前无调用方使用但违反分层契约）——
   现乘回 contractSize 换算为币数。新增 2 用例锁定。
3. **即时开仓死缓冲（已修）**：_execute_open 增加 buffer_notification 参数，即时开仓路由
   传 False——此前该路由的开仓通知滞留汇总缓冲区，直到下次日检开头被静默清空（靠巧合不重复）。
   tests/ 桩同步断言新语义。
4. **manual_check 防跨站（已修）**：要求 JSON body（空 {} 即可），与其余写接口同等门槛；
   前端与测试均无该接口调用方，唯一影响是裸 curl 需带 -d '{}'。
5. **通道展示兜底口径（已修）**：/api/channel_data 的 channel_period 兜底 32 → 28，
   与 TurtleStrategy 默认一致（仅在 config 缺字段时生效，展示层）。
6. （时区部署说明经核对已在收官轮补入 README 第 77 行，无需重复。）

本机 116 用例（两解释器双跑）+ 依赖版 50 用例全绿。
至此：从第一轮审查起的全部意见（含 Codex 各轮）均已闭环——修复、明确拒绝（附理由）或
压后（仅巨类拆分第二阶段，前置条件为模拟盘验证通过）三种终态，无悬空项。


---

## 架构拆分第二阶段（模拟盘前置条件解除后执行 / 云端 Fable 5 代跑）

第一阶段（stop_guardian / reporting）的既定收尾，照同一 mixin 模式机械搬移，
以 git 提交为单位、提交前必跑双套件验收：

1. **signal_handlers.py（SignalHandlersMixin，策略信号分派）**：
   handle_no_position_turtle / handle_open_position_turtle / handle_open_signal_turtle /
   check_and_update_stop_loss_turtle / handle_no_position_ma_cross / handle_open_position_ma_cross。
2. **trade_executor.py（TradeExecutorMixin，下单执行）**：
   _flip_position / _persist_open_position_or_rollback / _execute_open /
   _update_stop_order / handle_close_signal。

约束与验证（与第一阶段同标准）：
- 方法体**逐字节**平移（diff 逐行核对为空），零逻辑修改、零重命名、日志字符串与原文
  完全一致；方法仍绑定 TradingSystem 实例（self 语义/测试对实例方法的桩打法不变）。
- 新模块只依赖标准库 + trade_state，不 import main/ccxt/pandas——_test_stubs.import_main()
  桩机制不受影响；mixin 之间经 self 互调（如 handle_close_signal → handle_open_signal_turtle）。
- **测试零改动**：本机 116 用例 + 依赖版 50 用例在每次提交前全绿，未改任何断言。
- main.py 1228 → 631 行，保留装配与配置、状态迁移与归属护栏、启动同步、
  日检总指挥 check_and_execute_trades 与调度注册（真钱编排核心，刻意留在 main 便于审查）。

⚠️ 部署提醒：部署包自此必须包含 signal_handlers.py、trade_executor.py
（连同第一阶段的 stop_guardian.py、reporting.py），缺文件 main 导入失败直接拒启。
回滚：搬移不含状态迁移，git revert 对应提交即可一步回滚。

至此巨类拆分两阶段全部闭环：main（装配/调度）+ 四 mixin（止损防线/通知报表/
信号分派/下单执行），检查器只剩 check_and_execute_trades 一处编排入口。


---

## 收尾批次（行为增强 7 项 / 云端 Fable 5 代跑，用户逐项指定）

不同于机械搬移轮，本批为行为增强：只改列明的点，每项独立提交，
提交前必跑三条验收（py_compile 全源 / 本机套件 / 依赖版套件）。

1. **日检兜底任务**：_run_startup_catchup_check 注册为 okx_daily_check_fallback
   （cron */30，max_instances=1, coalesce=True）——主执行+重试整窗失败（如恰逢
   网络故障）后当日仍有触发点。守卫幂等直接复用；「启动补跑」→「兜底补跑」
   为本批唯一文案改动。新增 3 用例（注册参数 / 到点未跑触发 / 已跑不触发）。
2. **时区守卫**：启动时（notifier 可用后）校验本地 utcoffset == +8h，不符
   critical + 钉钉告警一次、不阻断。刻意不给调度器钉时区——date.today()/T+1/
   求索指数切日全用本地时间，单独钉会造出双时钟系统。
3. **适配层收口**：ExchangeApi 新增带重试的 get_last_price（内部/ccxt 符号归一，
   失败上抛），7 处 .exchange.fetch_ticker 穿透直调全部机械替换，各处回退逻辑
   原样保留。测试桩配套补 get_last_price（委托同一 fetch_ticker mock，断言零改动）；
   其中 InstantOpenConfigRollbackTests 缺桩时 500 断言走错路径「碰巧通过」，
   补桩恢复其原验证意图。
4. **启动权益失败先告警再退出**：三连败 sys.exit(1) 前 try/except 补发钉钉
   （与账本损坏路径同标准，杜绝静默死亡）。
5. **手动平仓路由去重**：api_server.close_position 手写补偿块替换为
   _close_trade_state_with_runtime_fallback(symbol, price, "手动平仓")；
   500 文案与成功路径响应不变，钉钉告警统一为收口方法格式。
6. **登录防爆破 + Cookie 加固**：按 IP 连续 5 次失败锁 60 秒（成功清零，内存级）；
   SESSION_COOKIE_SAMESITE='Lax'；SECURE 由 TRADING_COOKIE_SECURE=1 控制
   （不写死，保内网 HTTP 可用），README 部署表补行。新增 2 用例。
7. **依赖锁定（仅文档）**：README 注明上线机器验证通过后在该机器 pip freeze
   固化 requirements.lock——锁文件必须来自实际运行环境，不在 CI/开发机生成。

批后规模：本机 119 用例 + 依赖版 52 用例全绿。
另：仓库凭据经用户确认为模拟盘（demo），轮换与历史重写解除，
7a0d3c2 的「从今往后不跟踪」即为终态。


---

## 资金变动同步的时效性修复（用户反馈驱动）

**问题（用户准确回忆的"时效性很短"）**：同步以「最近一次已记录的指数值」为锚，而权益
采样每 5 分钟一次——入金到账后若采样先跑，会用旧除数把入金记成一根"盈利"暴涨点；
此时再点同步，锚恰好取到被污染的值，入金被永久算进指数。安全窗口只有到账后的
第一个采样周期（<5 分钟）。

**修复**：`equity_sync(flow_amount=None)` 支持净变动金额（入金为正/出金为负）——
提供时锚点按「(当前权益 − 净流入) ÷ 旧除数」精确反推，点击时机不再影响正确性；
反推出的变动前权益 ≤0 时拒绝（防正负号/数量级填错）。留空维持旧行为（时效窗口内
点击仍正确）。前端资金管理卡新增金额输入框与两种锚定方式的说明，确认框显示将采用
的锚定方式。API /api/equity_sync 接收可选 flow_amount，钉钉通知带锚定方式。

**顺带修复既有 bug**：全新系统（initial_equity 为 None，从未跑过统计刷新）时点同步，
原实现会在日志 :.2f 格式化处 TypeError → 路由 500。现 or 0 兜底。

新增 3 用例（污染 tick 后精确校正 / 不合理金额拒绝 / 留空旧行为）。
本机 122 用例 + 依赖版 52 用例全绿。


---

## 180% 终审轮（Fable 5，逐行读完两个搬移 mixin + 全 main.py + 路径炸弹扫描）

**逐行复核**（此前从未完整读过的部分）：stop_guardian.py / reporting.py 两个第一阶段
搬移 mixin，与原文逐行一致、搬移无损；main.py 全文（装配/fail-closed 顺序/日检编排/
兜底调度）严密无误。

**"路径炸弹"系统扫描**（上轮 initial_equity=None 那类"从未被执行过的路径"）——
三处疑似逐一验证上游不变量后确认安全，未改（防御反成噪音）：
- record_daily_equity_snapshot 的 `qiusuo_index:.2f`：上游 ensure_qiusuo_index_state(persist=True)
  保证除数存在 + equity 已过滤，不可能为 None；
- equity_sync 的 `/ old_divisor`、`/ qiusuo_anchor`：除数恒正，pre_flow_equity>0 已守卫；
- 登录限流 _login_failures 字典理论上随失败 IP 增长——单用户面板可忽略，不引入清理复杂度。

**唯一修复（边界处理一致性）**：config.strategy 的 channel_period / default_risk_per_trade
在装配层是直接下标访问，缺失会抛裸 KeyError；而凭据缺失给的是干净 ValueError。
统一为前置校验 + 清晰 ValueError（fail-loud，不静默塞默认值——真钱系统默认策略参数
比拒启更危险）。ma_* 三键有 .get 默认值，不在此列。新增 2 用例（两键分别缺失）。

本机 123 用例 + 依赖版 52 用例全绿。至此全部审查轮次闭环，无悬空项。


---

## 采纳 Codex 补充：启动路径补齐范围校验（边界一致性收尾）

Codex 独立复核 2196953 后指出：该修复只校验策略必需键"缺不缺"，未校验"合不合法"——
手写 config.json 的 channel_period=0 / default_risk_per_trade=-1 等非法值仍能带病启动
（通道计算崩溃 / 以损定量产出负仓位）。前端与 API 改参已有范围校验，但文件边界的启动
校验没有同等防护。采纳。

- 抽 `main._validate_strategy_config`：必需键存在 + 范围合法，口径与
  api_server.update_strategy_params / MAX_RISK_PER_TRADE **完全一致**（周期整数 [2,500]、
  默认风险度 (0,50%]、EMA 短期<长期），注释锚定两处口径须同步，防未来漂移；
- 有意保留两处校验器（文件边界 vs 接口边界各自自卫），不重写已被 52 用例覆盖的 API
  路由——去重虽更优雅，但改动真钱相邻的受测路由，风险超过收益；
- 新增 test_out_of_range_strategy_param_rejected（7 个非法场景：周期上下限、负/零/超限
  风险度、短≥长）。

本机 124 用例 + 依赖版 52 用例全绿。


---

## 采纳 Codex 三条（P1 品种池校验 / P2 类型规范化 / P3 严格整数）

三条实为同一件事——把"手写 config.json"这条入口在启动时收口到与前端/API 完全一致，
补上此前只做了 strategy、漏掉 trading.symbols 的最后一块。

- **P1（真安全缺口，已修）**：新增 `_validate_symbol_configs`，与
  api_server._validate_symbol_input 同口径校验品种池——交易对名 `^[A-Z0-9]{1,20}USDT$`、
  risk_per_trade (0,50%]、策略白名单 turtle/ma_cross，另加重复品种检测。此前手写
  risk_per_trade=1.0（100% 全仓风险）能启动、非法策略名静默落海龟，现启动即拒。
  risk_per_trade 缺省仍合法（由 default_risk_per_trade 兜底，既有行为）。
- **P2（比 Codex 判断更严重，已修）**：校验后规范化写回类型。原实现 int()/float() 只
  校验不写回——"28"/"0.01" 字符串能通过却仍是字符串，构造 TurtleStrategy("28") 会在
  盘中 `int < str` TypeError、RiskManager 权益×"0.01" TypeError（运行时崩溃，非仅口径）。
  现 strategy 周期写回 int、风险度写回 float、品种 risk_per_trade 写回 float。
- **P3（已修）**：新增 `_strict_int`——28.9/"28.9" 拒绝而非静默截断为 28（fail-loud）。

**有意保留的一处不对称（诚实声明，非疏漏）**：api_server.update_strategy_params 仍用
`int()`（28.9→28 截断），未改——它是 UI 数字输入入口、被 52 用例覆盖，Codex 亦明确 P3
"不是立即下错单的红线"；启动校验更严（拒绝）是安全方向，不为对齐而改动受测的真钱相邻路由。

新增 3 组用例（品种池 6 非法场景 / 字符串类型规范化 / 小数周期拒绝）。
本机 127 用例 + 依赖版 52 用例全绿。


---

## 三入口口径收敛为单一事实源（收掉 Codex 唯一"以后可做"项）

Codex sign-off 后唯一标记的残留项：api_server 的 update_strategy_params 仍用 int() 截断
28.9→28，与启动校验"拒绝小数"不一致。深审确认这不是隐藏地雷（API 只写规范化后的合法值，
从不删必需键，故 API 写出的 config 启动全量校验必过——不存在"API 能过、重启拒启"的情形，
纯外观不对称），但它破坏了"三入口一致"这个反复声明的设计属性。收掉：

- 新增 **config_validation.py**（零依赖，仅 re）：把校验叶子原语收敛于一处——
  PERIOD_MIN/MAX、MAX_RISK_PER_TRADE、SYMBOL_RE、STRATEGY_WHITELIST、strict_int。
  三入口（前端表单 / HTTP API / 手写 config.json）的一致性自此由**构造保证**，
  不再靠人工同步两份常量。
- main.py：删除本地常量与 _strict_int 副本，改用 cfgv.* ；顺带移除不再使用的 import re。
- api_server.py：_SYMBOL_RE / MAX_RISK_PER_TRADE 改为从 config_validation import；
  update_strategy_params 的 int() 换成 strict_int——**API 现在也拒绝小数周期**（P3 完全闭合）；
  移除不再使用的 import re。
- 各入口的编排（启动校验全量 config vs API 校验增量 delta）形态不同，仍各自保留，
  不强行捏成一个更难读的函数——只共享叶子原语，这是正确的抽象边界。

验证：strict_int 边界自证（28/28.0/"28"→int，28.9/"28.9"/"abc"/None→拒绝）；
三模块同时导入无循环；本机 127 + 依赖版 53 用例全绿（新增 API strict_int 拒绝小数 1 用例）。
至此 Codex 全部意见闭合，无任何开放观察项。


---

## strict_int 非有限数边界（采纳 Codex P3，一处修复覆盖三入口）

Codex 发现：strict_int 里 float("inf")/float("-inf") 能成功，但随后 int(f) 抛的是
OverflowError（非 ValueError），绕过 API 的 (TypeError, ValueError) 捕获 → 畸形输入
"inf" 变成 500（启动路径则崩成 OverflowError traceback）而非干净 400/ValueError。

修在共享的 strict_int 一处（math.isfinite 前置挡下 inf/-inf/nan）——**三入口同时闭合**，
正是上一轮抽取单一事实源的价值兑现：一处修复，前端/API/启动全覆盖。
（nan 原本恰好走 ValueError，现统一在 isfinite 处更早拦下；风险度 float 路径的 inf/nan
本就被范围比较自然拒绝，无需改动。）

自证：inf/-inf/nan/28.9/abc → 干净 ValueError，28 → 28。startup smoke 的小数周期用例
扩展覆盖 inf/-inf/nan。本机 127 + 依赖版 53 全绿。


---

## 外部输入规范化一族一次性收敛（采纳 Codex 3×P2，不再逐叶子补）

Codex 的元判断正确：前几轮每次只修一个"叶子"（整数→inf→config 键），真正该一次收敛的是
"所有外部输入统一规范化"。本轮把布尔、有限浮点、符号名、风险度全部收口到 config_validation，
三入口（前端/API/手写 config）由同一层保证一致。

- **P2-1 enabled 字符串陷阱**：Python `bool("false")==True`——手写/直调 `"enabled":"false"`
  会被当启用继续开仓。新增 `strict_bool`：真 bool 原样、"true"/"false"（不分大小写）解析、
  其余拒绝。启动校验与 API（add/update）均规范化 enabled 为真 bool。
- **P2-2 risk_per_trade 只验不规范化**：直调传 "0.01" 通过校验但仍是字符串，进 _execute_open
  仓位计算/update_symbol 消息格式化时 TypeError→"配置已写、接口 500"的不一致。
  `_validate_symbol_input` 改为返回 (clean, error)，三条路由（add/update/instant_open）
  全部用规范化后的 float/bool/大写 name 写入 config。
- **P2-3 flow_amount 非有限数**：`equity_sync(nan)` 写出 nan 除数、`(-inf)` 写出 0.0 除数，
  污染求索指数。新增 `strict_float_finite`；API 路由与 equity_tracker.equity_sync 内部
  双重把关（状态边界 fail-closed）。
- **符号名统一** `normalize_symbol_name`：非字符串显式拒绝（防 .upper() 的 AttributeError→500）、
  去空格转大写后校验。启动与 API 同源，手写小写/带空格自动规范化。

顺带：strict_int 复用 strict_float_finite（inf 拦截逻辑收敛一处）；_validate_strategy_config
的 default_risk_per_trade 改用 strict_risk_per_trade。

新增测试：test_config_validation.py（5 组原语边界，纯标准库）；startup 3 例（规范化写回 /
"false"→False / 非法品种含非字符串名·inf 风险·歧义布尔）；API 4 例（规范化契约 / 非字符串名 /
歧义 enabled / flow_amount nan·inf·-inf 拒绝）。本机 139 + 依赖版 57 全绿；四模块无循环导入。


---

## 复审加固（Opus 4.8 全量复审：调度配置纳入单一事实源）

对最新代码逐字复审后，发现「配置校验单一事实源」收敛遗漏了 config.scheduler 块：
check_hour/check_minute/summary_*/weekly_*/stop_loss_scan_interval_minutes 这些整数
直接流入 register_jobs 的算术（check_minute + 1）与格式化（{check_hour:02d}）以及
_run_startup_catchup_check 的 (check_minute + 2)，此前无任何校验——手写 config.json
把 check_minute 写成字符串 "0" 会让 `check_minute + 1` 抛 TypeError、把 check_hour
写成 25 会让 APScheduler 抛内部错，都是难懂的启动崩溃而非清晰的 ValueError。

补 main._validate_scheduler_config（与 _validate_strategy_config/_validate_symbol_configs
同源 config_validation、同标准 fail-loud）：小时 [0,23]、分钟 [0,59]、
巡检间隔 [1,1440]，strict_int 规范化写回（"8"→8，杜绝字符串进算术），
全部可选键仅在显式提供时校验。至此 config.json 的全部数值入口
（strategy / trading.symbols / scheduler）口径完全一致。

新增 2 用例（test_startup_smoke）：非法调度值启动即拒 / 字符串数值规范化为 int。
验收：py_compile ✓；本机 143 用例 + 依赖版 59 用例全绿。


---

## 终局复审（Opus 4.8 逐字通读全仓：孤儿止损单清扫）

用户要求一轮最彻底的最终审查。逐字通读全部真钱路径（okx_api 的开/平/止损/撤单确认、
trade_executor 的开仓与回滚、signal_handlers 的两策略分派、stop_guardian 三防线、
trade_state 事务、equity_tracker 求索指数数学）后，结论：核心经得起终局审查，
仅发现并修复一处低概率但真钱会出事的潜藏缺口。

**缺口：_execute_open 「止损单创建失败」回滚路径可能残留孤儿 reduce-only 止损单。**
create_stop_loss_order 返回 None 的一种情形是「下单超时→实际已在交易所创建成功→
复查因触发价被交易所按 tick 取整、与我方原始价差超 1ppm 未匹配上」。此前该分支只
close_position 平仓、不撤挂单，那张孤儿 reduce-only 单本地无记录；止损自愈巡检只查
「本地记录的止损单是否还在」，检测不到「多余的」孤儿单（盲区），会在该品种下次开仓时
错价触发误平新仓。对 tick/价格比 > 1ppm 的便宜币种真实可达。

修复：回滚平仓前先 cancel_all_orders 清扫该品种全部挂单（回滚要平的就是本次仓位，
撤光挂单幂等且安全）。新增断言：清扫必须在回滚平仓之前（call order）。

其余回滚点（handle_close_signal / _flip_position / _handle_exchange_flat_close /
_persist_open_position_or_rollback）均已各自 _cancel_stop_order_confirmed 撤掉已知
止损单，无同类盲区；_execute_open 成交后止损越位回滚发生在建止损之前，无孤儿。

验收：py_compile ✓；本机 143 用例 + 依赖版 59 用例全绿。


---

## 部署后风险扫描（Opus 4.8：调度校验区间与 cron 消费口径不一致）

用户部署并通过初测后，要求再全盘扫描一轮未发现的风险。逐字复读全部真钱路径
（okx_api 张数换算/超时确认/验证式撤单、trade_executor 开仓回滚、signal_handlers 两策略、
stop_guardian 三防线、trade_state 事务、equity_tracker 求索指数数学、config_validation 三入口、
api_server 全路由、前端 app.js、wsgi 单实例锁、mem_monitor、verify_okx、CI、.gitignore）后，
核心经得起复审；顺带核实并排除两处疑似风险：
- ccxt 4.5 的 okx `parse_position` 用 `Precise.string_abs(pos)`，`contracts` 恒非负——
  下游 `contracts > 0 / == 0` 口径安全（适配层 abs 与下游一致），不是 bug；
- `_stop_anomalies` 无锁但 get_status 用 C 级 `dict()` 拷贝（GIL 内原子），非竞态。

**发现并修复一处真实缺陷（HIGH，此前所有审查轮次漏掉）：**
`_validate_scheduler_config` 放行 `stop_loss_scan_interval_minutes ∈ [1,1440]` 并规范化写回 int
（判定「合法」），但 `register_jobs` 一律用 cron `minute='*/N'` 消费它——实测 APScheduler
的 `CronTrigger(minute='*/60')` 直接抛 `step value (60) higher than the total range (59)`，
任何 N≥60 都在 `add_job` 抛异常。该异常发生在 `start()`，而 start 跑在 wsgi 守护线程里：
异常让整个调度注册崩溃、交易线程静默死亡——**Web 面板照常，日检/盘中巡检/权益采样一个都不注册**，
真钱系统「看起来在线、实则完全不托管」。讽刺的是，这个校验轮次的初衷正是"给清晰 ValueError
而非难懂的启动崩溃"，却因区间与 cron 语义不匹配制造了它想消灭的那种崩溃。

**漏检根因**：标准库套件把 `BackgroundScheduler` 换成 `Dummy` 桩（add_job 空转、不校验
cron 表达式），`test_register_jobs_smoke` 跑在假调度器上，永远触不到真实表达式校验。

**修复**：cron 分钟步长天然只支持 [1,59]——N≤59 保持原 cron（对齐 :45 秒，零行为变化）；
N≥60 改用 `interval` 触发器（`minutes=N`），覆盖校验放行的 [60,1440] 全区间，让校验与消费口径
一致。默认值 5 不受影响（纯预防性加固）。

**测试**：依赖套件新增 SchedulerIntervalTests 2 用例（真 BackgroundScheduler：≥60 走 interval
触发器且注册不崩 / ≤59 仍走 cron）——这条真实崩溃路径只能在装了 apscheduler 的依赖套件验证；
标准库套件补 test_large_scan_interval_passes_validation（锁定 60/120/1440 通过启动校验并规范化）。

验收：py_compile 全源 ✓；node --check app.js ✓；本机 152 用例 + 依赖版 62 用例全绿。


---

## 部署后风险扫描·第二轮（Opus 4.8：ma_cross「永远在市」缺口 + 三处安全加固）

用户要求以最高强度再逐字全盘扫描一轮。逐条实证复核了上轮标记为「安全」的关键点
（ccxt `amount_to_precision` 向下截断不放大仓位、`contracts` 恒非负、锁序 T→E→S 无环、
裸仓不变量、未创新高结算幂等、08:00 切日时区无关、求索指数除数全程正数守卫），无翻案。
本轮据用户批准修 4 项（1 项策略语义、3 项安全加固）：

**P2（策略语义，用户批准）ma_cross「永远在市」在开仓腿失败时的缺口**：
`_flip_position` 反手开新腿失败、初始金叉/死叉开仓失败、T+1 重入开仓失败——三处此前失败后
不留恢复线索（翻转/初始不记 T+1；T+1 重入无条件删标记），该品种会一直空到下一次全新 EMA
交叉，违背「永远在市」。修复：统一收口 `_mark_ma_cross_reentry_pending`（记 T+1 + 告警），
三处开仓腿失败均记 T+1 交由次日 handle_no_position_ma_cross 按当时 EMA 方向自动重入；
T+1 重入失败改为**保留**标记次日重试（成功才删）。成功路径逐字不变，只补失败恢复。
（依赖套件 test_symbol_removal_management 的 flip 桩此前不完整——_execute_open 桩不建仓，
get_open_position 返回 None 会被新的失败检测误判，已补桩为「成功即 add_open_position」。）

**P3 安全加固**：
- `/api/qiusuo_index_ohlc?days=<超大值>` 无上限 → `datetime.now()-timedelta(days)` 抛
  OverflowError → 500。钳制 `max(7, min(days,100000))`（≈274 年对任何真实数据集等价「全部」）。

**P4 安全加固**：
- 登录密码改 `secrets.compare_digest`（恒定时间，消除计时侧信道，与 API Token 同口径）。
  **关键**：必须 `.encode('utf-8')` 成 bytes 再比——compare_digest 对 str 仅支持 ASCII，
  含中文等非 ASCII 密码用 str 会抛 TypeError 令登录彻底失效（差点自造回归，已实证并规避）。

**如实声明·本轮有意不改的观察项**（已报告用户，非疏漏）：
- `create_stop_loss_order` 内部超时重试在触发价 tick 取整 >1ppm 时极低概率产生重复/孤儿止损单
  ——reduce-only + 止损自愈 + 残留清理已兜底；改动最红的适配层重试逻辑风险大于收益，不动。
- `ProxyFix(x_for=1)` 依赖单层反代拓扑（README 已声明）；`closed_trades` 无限增长为长期规模项。
- 标准库套件对 OkxApi/策略/调度器/notifier 全打 Dummy 桩，集成路径（真 cron/ccxt）测不到——
  上轮调度僵死正因此漏检，凡涉真依赖的改动务必在依赖套件补测。

验收：py_compile 全源 ✓；node --check app.js ✓；本机 152 用例 + 依赖版 65 用例全绿。


---

## 部署后风险扫描·第三轮（Opus 4.8：全模块攻击面深挖——钉钉 access_token 日志泄露）

用户要求继续深挖全模块、有问题就修。本轮转向此前只"路过"的攻击面：命令注入、
凭据进响应/日志、原子写数据丢失窗口、适配层查询降级误判、日志脱敏。定向扫描结论：
- **无命令注入**：mem_monitor 的 subprocess.run 为固定参数列表（无 shell、无用户输入）；
  全项目无 eval/exec/os.system/pickle。
- **凭据不进响应**：/api/config 只回 strategy/trading/scheduler，okx 凭据从不序列化到任何响应；
  无路由整体返回 config。
- 原子写 `.bak` 顺序保证掉盘不丢命脉；`persist_config`/`reload_strategies` 引用交换 GIL 原子无撕裂；
  `_stop_anomalies` 的 `dict()` 拷贝 GIL 原子无迭代竞态——均复核安全。

**发现并修复一处真实密钥泄露（本轮新增）：钉钉 webhook access_token 会进日志。**
`DingTalkNotifier.send_message` 在网络异常时 `last_err = str(e)` 并 `logger.warning/error` 落盘。
实测 `requests` 的连接/超时异常字符串**带完整 URL**（`...robot/send?access_token=xxx`），
而这正是走 except 分支的时机（DNS/代理/断网）。webhook token 是可轮换密钥，进 trading.log
后经 /api/logs 面板与备份泄露——README 明确要求泄露即轮换。mem_monitor.send_dingtalk 的
`logger.error(f"...{e}")` 同源同病。

修复：两处各加 `_redact_secrets`（正则 `access_token=[^&\s'"]+` → `access_token=***`），
异常落日志前先抹 token。新增 DingtalkRedactionTests 2 用例（模拟真实 ConnectionError 串
被脱敏 / 普通文本不受影响，依赖套件——dingtalk_notifier 顶部 import requests，标准库套件跑不了）。
（历史日志中可能已有的 token 属旧数据，本修复只堵未来泄露；如在意可轮换一次 webhook。）

**如实声明·仍有意不改的已知残留**（非新增、已多轮论证）：
- `_fetch_algo_orders` 三参数组合"某组合成功但空"的理论误判：已 merge+全失败才 raise，
  reduce-only + 止损自愈 + verify_okx 沙盒实测兜底；改最红适配层查询策略风险大于收益。
- `create_stop_loss_order` 超时重试极低概率重复挂单；ProxyFix 单反代拓扑；closed_trades 长期增长。

验收：py_compile 全源 ✓；node --check app.js ✓；本机 152 用例 + 依赖版 67 用例全绿。


---

## 部署后风险扫描·第四轮（Opus 4.8 终局：API Token 非 ASCII 头 → 500 一致性缺陷）

用户要求「终局之战」再逐字深挖全模块。本轮把此前只"读过"未"逐边界质证"的部分全部追死：
- 海龟 `_build_channel_lines` 窗口口径（含当前 bar 排除）、`_find_latest_mid_cross_index` 的 None
  边界（range 停在 channel_period 不触 None）、bootstrap 直通资格链；
- 双均线金叉/死叉的 `<=`/`>=` 相等边界（触碰不触发、无重复触发）；
- 手续费 `calculate_closed_trade_metrics` 多空/亏损方向；
- `instant_open`/`close_position` 锁获取（acquire 在 outer try、work 在 inner try/finally，无泄漏）；
- 求索指数 `_resolve_qiusuo_history_entry`/`_normalize_qiusuo_index_state` 坏数据过滤；
- `_compact_closed_ticks` 先存 OHLC 后删采样的保序、时钟倒退 `max(0, .days)` 守卫；
- `get_channel_data` dropna 后空 df 的 `iloc[-1]`（有 `len>0` 守卫）；
- `_last_check_date` 快路径无锁读但 check_and_execute 锁内权威复核防重复执行；
- **并顺带复核确认上轮"#2 超时重试重复挂单"实际很遥远**：策略止损价均来自收盘价、
  天然 tick 对齐，发出即被交易所原样接受，超时确认在 1e-6 容差下稳定匹配、不产生重复单
  （上轮我一度高估，本轮修正）。

**发现并修复一处真实缺陷（与上轮密码修复同源、漏掉的一致性坑）：**
`require_auth` 的 API Token 比较用 `secrets.compare_digest(token, API_TOKEN)`——两个 str。
compare_digest 对 str 仅支持 ASCII：攻击者发一个**非 ASCII 的 X-API-Token 头**会抛 TypeError，
而装饰器内无 try 捕获 → Flask 返回 **500 而非干净 401**，还能借 500/401 差异探测「token 是否
启用」。上轮把 api_login 密码改成了 bytes 比较，却把同一个坑留在了 token 路径。
修复：`token.encode('utf-8')` / `API_TOKEN.encode('utf-8')` 后比较（bytes 无 ASCII 限制，
与密码路径同口径）。新增 ApiTokenAuthTests 2 用例（非 ASCII token 干净 401 / 错误 token 401）。

**结论**：四轮扫描至此，真钱核心（止损三防线、账本事务、以损定量、求索指数数学、
配置三入口、两策略状态机）逐字追死，无未修的交易安全临界项；本轮仅剩的这处为认证
健壮性/一致性缺陷（非 auth 绕过），已闭环。其余残留（适配层查询降级、closed_trades
长期增长、ProxyFix 单反代）经理性权衡维持「非代码控制」（沙盒验证/监控触发/部署纪律）。

验收：py_compile 全源 ✓；node --check app.js ✓；本机 152 用例 + 依赖版 69 用例全绿。


---

## 全仓再审·第五轮（Fable 5：「艺术品」标准——修 4 / 清 8 / 磨 10）

用户要求以「内部没有一丝瑕疵」的标准再全量审查。逐字重读全部生产模块、前端、
配置、CI 与三份文档后，本轮按风险从高到低执行：

**正确性修复（4）**：
1. **TRADING_SKIP_STARTUP_CATCHUP_ONCE 缺时间窗（HIGH）**：上轮新增的部署跳检护栏
   无条件把当天标记为已日检——若重启发生在 08:00 **之前**（如清晨滚动代码），标记会让
   当天正点日检被 `_last_check_date` 拦截，整日新信号与止损推进丢失。现只在已过今日
   检查窗口（与启动兜底同一「绝对分钟数 + 2 分钟缓冲」阈值）时生效，未到点时标志忽略
   并告警说明原因。`_run_startup_catchup_check` 的「今日已跑」判定同步改用注入的
   `now.date()`（消除测试注入时间与真实日期的错位）。
2. **写路由 JSON null → 500（P3）**：add_symbol / update_symbol / instant_open 对显式
   `"risk_per_trade": null` 等值——`.get` 默认值只覆盖「键不存在」，null 穿过校验被视为
   「未提供」，随后 `clean[键]` KeyError → 500。现 null 与缺省统一按未提供处理（add/
   instant 落默认值）；update_symbol 改以 clean 判断提供了哪些字段，三字段全空时返回
   清晰 400（原为无操作 200 + 空变更钉钉）。
3. **instant_open 策略与品种池冲突（P2）**：品种池已有 BTCUSDT=ma_cross 时按 turtle
   即时开仓，日检对在池品种一律按池内策略托管——开出的仓下一轮即被另一套止损/出场逻辑
   接管。现池内策略与请求不一致直接 400（与「有持仓禁改策略」同一护栏逻辑）。
4. **每日持仓汇总风险口径虚增（P3）**：notify_position_summary 用 `abs(entry−stop)×size`，
   止损已推进到盈利侧（趋势跟踪常态）时把锁定利润虚报成风险，「当前实际风险度」失真。
   现改为有向亏损钳到 ≥0，与前端持仓面板 `Math.max(0, …)` 同口径。

**死代码清除（零行为变更）**：notifier 单条开/平仓通知（汇总版取代后无调用方）、
trade_state 的 update_last_check_time / mark_mid_line_crossed / 默认态 last_check_time、
ma_cross_strategy 未使用的 pandas/numpy 导入、EquityTracker 死参数 send_message、
main 的重复 ccxt_sym 与函数内 import sys/shutil 上提、app.js 多所遗迹
exQuery()（无参恒空）/ exLabel()（恒「欧易」）。

**一致性打磨**：
- 日检循环品种池改为轮次开始时快照（与盘中巡检 `_reconcile_symbol_intraday` 同模式）：
  消除逐品种重扫池子的 O(n²) 与循环中途 API 增删品种的同轮视图漂移；
  handle_no_position_turtle 多空对称分支去重（日志与通知字符串逐字不变）。
- equity_tick_retention_days 补启动校验（strict_int + [7,3650]，与 strategy/scheduler
  同标准 fail-loud；此前非法值被 EquityTracker 静默吞掉，配置值与生效值悄悄不一致）。
- 权益采样间隔单一事实源：register_jobs 的 cron 表达式与采样告警文案统一引用
  EquityTracker.EQUITY_TICK_INTERVAL_MINUTES（不再散落三处硬编码 5）。
- 止损触发通知出场价统一传原始数值（日检两处原为 .4f 字符串、巡检为原值，口径不一；
  低价币 .4f 会截掉有效位）。
- api_server 路由体内 trading_system 全局引用统一为局部 system（同对象，消除混用）；
  get_channel_data 双 dropna + 逐行 apply 简化为向量式；okx cancel_all_orders 失败
  返回 False（原 None，契约模糊）；retry 装饰器可变默认参数 [1,2,4] 改元组。
- README / 迁移说明测试计数刷新（143+59 → 156+72，含逐模块数）；运维清单补跳检标志
  时间窗说明。

**本轮复核后维持不动的既有裁决**（无翻案）：适配层查询降级、closed_trades 长期增长、
ProxyFix 单反代、登录限流字典增长——理由同前几轮，不再赘述。

新增测试 5：跳检未到点失效（含正点日检不被吞）/ retention_days 校验（本机 2）；
instant_open null 默认值 / instant_open 策略冲突 / update_symbol 全 null 400（依赖版 3）。
验收：py_compile 全源 ✓；node --check app.js ✓；本机 156 用例 + 依赖版 72 用例全绿。


---

## 部署伴随修复：测试套件不再污染生产 trading.log

实盘部署验证时发现：在部署机上跑测试套件，模拟场景（止损残留、CRITICAL 告警等）
会写进真实 trading.log——main 导入时的 logging.basicConfig 把 RotatingFileHandler
挂上根 logger，测试进程照单全收，真假告警混淆监控视野。

修复（纯测试基建 + 一处等价的生产日志参数，零交易行为变更）：
- _test_stubs.import_main 与依赖套件在 import main 前给根 logger 挂 NullHandler——
  basicConfig 仅在根 logger 无 handler 时生效，测试进程内自然空转；
- main 的 RotatingFileHandler 加 delay=True（懒打开）：构造时不再凭空建文件/占句柄，
  首条日志时才创建——生产启动毫秒内即写日志，行为等价。

验证：干净树跑两套件（156 + 72 全绿）后 trading.log 不创建、不写入；
delay=True 首条日志即建文件写入自证通过；pyflakes 全仓仍归零。


---

## 保留项消除轮（用户命题：三项故意保留的东西能否不保留）

对三项历史保留项逐一评估「可消除性」后，按用户确认执行两项，第三项维持并说明：

**① closed_trades 无限增长 → 已消除（账本/史书分离）**：
交易历史作为数据必然累积，消除的是增长的全部代价——命脉账本 trade_state.json
只保留最近 KEEP_RECENT_CLOSED(200) 条平仓记录，每日检查开头把超窗部分归档进
只追加的史书 closed_trades_archive.json（.gitignore 已排除）。账本从此恒定大小，
每次落盘不再全量重写逐年增长的历史。安全语义分层：账本维持 fail-closed；
史书损坏只降级（展示近期、归档暂停并告警），绝不阻断启动、绝不丢账本数据；
「史书已写、账本落盘失败回滚」的窄窗口由内容级去重消除重复。
get_closed_trades() 返回史书+近期合并视图，API/前端零改动。
新增 test_closed_trades_archive.py 8 用例（归档顺序/重载持久/幂等增量/史书损坏
降级/写失败保账本/窄窗口去重/日检接线）。

**② ProxyFix 单反代假设 → 已消除「写死」，残余降至理论下限**：
反代跳数从硬编码 x_for=1 改为 TRADING_PROXYFIX_X_FOR 环境变量声明
（0=直连不信 XFF / 1=单反代或 Cloudflare Tunnel（默认）/ 2=CDN→nginx 双层），
非法值拒绝启动（fail-loud，_parse_proxyfix_hops + 2 用例）。代码无法安全地
自动探测代理深度（盲信 XFF 本身就是漏洞），「部署方须如实声明拓扑」是该问题的
理论下限。实盘核实：用户为 Cloudflare Tunnel 架构（源站零入站端口 + CF Access
邮箱验证），默认值 1 恰为正确配置，无需运维动作。

**③ 适配层查询降级盲区 → 暂维持保留（消除路径已明确，待用户决策）**：
彻底消除须钉死 OKX 原生算法单端点（绕过 ccxt 统一接口的跨版本参数漂移），
属最红路径改动，按本仓铁律必须先过模拟盘全链路验证后方可上实盘。
现有三重兜底（验证式撤单+残留阻断 / 5 分钟止损自愈 / reduce-only 损失有界）不变。

验收：py_compile ✓；pyflakes 全仓归零 ✓；本机 164 + 依赖版 74 用例全绿；
README/迁移说明/运维清单计数与说明同步。


---

## 保留项③消除轮（算法单查询钉死 OKX 原生端点）

历史保留项③（_fetch_algo_orders 经 ccxt 统一接口三参数组合猜谜+合并的理论盲区：
某组合可能因跨版本参数映射漂移「成功但答非所问返回空」，合并清单不完整会让验证式
撤单误判「已撤干净」）正式消除：

- **查询钉死原生端点**：`GET /api/v5/trade/orders-algo-pending`（ccxt 隐式方法
  privateGetTradeOrdersAlgoPending，已实证注册于 ccxt 4.5.64 的 okx private GET 表）。
  该端点是待触发算法单的唯一权威来源——问题只有一种问法，映射漂移在结构上不存在。
- **响应信封校验**：code=='0' 且 data 为数组才接受，否则抛出（对「成功但答非所问」
  的结构性防护——空 data 是权威的「无挂单」，畸形信封绝不当空清单）。
- **严格性升级**：ordType 全集（conditional/oco/trigger/move_order_stop，前者为
  系统自建止损、后三者覆盖人工挂单供清扫路径可见）任一查询失败（各自带网络重试）
  即整体抛出——绝不基于可能不完整的清单下「不存在」裁决；旧实现「任一组合成功即接受」
  的可用性偏置改为正确性优先，调用方 fail-safe（跳过本轮/标记残留/阻断开仓）承接。
- **instId 确定性变换**（BTC/USDT:USDT→BTC-USDT-SWAP），不依赖 load_markets 缓存。
- **撤销路径刻意不动**：cancel 尝试仍走既有三参数变体——其成功标准本就以（现已权威的）
  查询列表裁决，实盘每日止损推进已验证该路径；改动面收敛到盲区本体。
- verify_okx.py 增补三态判定全链路检查（find_stop_order_state 应返回 intact），
  上线前验证同时覆盖新查询路径与严格匹配。

测试迁移与新增：适配层桩从 fetch_open_orders 全量迁移到原生端点形态
（_native_stop/_algo_stub/_phased_algo_stub），FetchAlgoMergeTest 重写为
FetchAlgoNativeTest 5 用例（全 ordType 覆盖合并/畸形信封抛出/任一类型失败抛出/
原生字段喂严格匹配器/instId 推导含内部符号归一）。本机 166 + 依赖版 74 全绿
（discover+倒序），pyflakes 归零。

**上实盘前置（用户选定小额实盘代替模拟盘，条件经确认）**：在系统未持仓、不在
品种池的品种上用 verify_okx.py 跑全链路（开微小仓→挂止损→原生查询+三态 intact→
验证式撤销→平仓），避开 08:00 日检时段；全绿后方可重启实盘服务。


---

## 实盘验证轮（LTCUSDT 小额实弹）：tick 取整缺陷证实并修复 + 撤销指令原生化

用户以小额实盘（LTCUSDT 1 张，多空双向）代替模拟盘执行 verify_okx.py 全链路，
经三条件确认（走 verify 脚本 / 非持仓非池内品种 / 避开日检时段）。结果：

**原生查询路径实弹通过**：两方向均「查到算法/条件单 1 个（原生 orders-algo-pending
端点）」、reduce-only ✓、验证式撤销 True、撤后清单 0、平仓无残留。

**实弹揪出真实缺陷（此前多轮标记为「理论上遥远」的场景被证实）**：
挂单价 39.384/48.114，交易所按 tick(0.01) 取整存储为 39.38/48.11——
find_stop_order_state 返回 mismatch 而非 intact。严格匹配 1e-6 容差对 tick 取整
失明；「策略止损价来自收盘价天然对齐」的历史论据是脆弱假设（交易所调整品种
tick 步长即失效），且同一失明会让创建超时确认误判 → 重复挂单（另一历史观察项）。

**修复：触发价 tick 对齐（_align_stop_price）**——ccxt price_to_precision 与 OKX
实盘取整行为已实证逐位一致（39.384→'39.38'）：
- 发单前对齐（create_stop_loss_order）：发送值与交易所存储值必然一致，
  超时确认匹配不再受取整干扰——历史观察项「超时重试极低概率重复挂单」一并消除；
- 比对前对齐（find_stop_order_state）：本地历史留存的非对齐价也能正确匹配；
- 精度元数据不可得时按原价返回（fail-safe 退化为对齐前行为）。

**撤销指令原生化（用户选定方案 A）**：_cancel_algo_order 的撤销指令从 ccxt 三参数
变体猜谜改为直调原生 POST /trade/cancel-algos（隐式方法与数组请求体已在 ccxt
4.5.64 实证）。指令返回依旧不构成结论，成败一律以原生查询清单裁决——查询与撤销
自此同一权威接口族，适配层对 ccxt 统一接口参数映射的依赖清零（仅市价开平仓与
K线读取仍走统一接口，其正确性由持仓查询确认/收盘过滤独立保障）。

测试：CancelAlgoVerified/CancelOrderFallback 组迁移到原生撤销桩（含请求体断言）；
新增 TickAlignmentTest 3 用例（发单对齐/比对对齐 intact/精度不可得回退）。
本机 169 + 依赖版 74 全绿，pyflakes 归零。

**上线前置**：用户须重跑 verify_okx.py LTCUSDT 1——两方向应全 ✓ 且
find_stop_order_state = intact；全绿后方可重启实盘服务。


---

## 实弹触发验证补全（verify_okx.py --fire 模式）

背景核查：确认「止损真实触发」此前在生产 trading.log 中**从未发生过**——
唯一疑似线索（7/5 21:39:40 附近的一批 WARNING/CRITICAL）经完整上下文核实
全部是当天首次在服务器上跑测试套件留下的痕迹（temp 目录路径、测试文件字面
异常文本、traceback 指向 test_symbol_removal_management.py），非真实事件；
日志无轮转（仅一个 168KB 文件），排除更早历史。用户选择方案 B：补一次
主动实弹验证能力而非仅接受交易所级承诺。

**新增 verify_okx.py --fire 模式**（需显式 `--side long/short`，不支持 both）：
止损挂在离市价很近处（默认 0.15%，可调），等真实行情自然触发（默认超时
300s），触发后断言三件此前从未被本系统亲眼验证过的事——持仓精确归零、
未反向开出反向仓（reduce-only 语义的核心承诺）、算法单从待触发列表消失
（列表滞后于触发生效时复查一次再裁决，与验证式撤单同一容忍）。超时未
触发判定为「不确定」而非失败（价格窗口内未走到，非系统问题），finally
保证清理不留残仓。复用既有 open_position/create_stop_loss_order（含
tick 对齐修复）/get_position/_fetch_algo_orders/cancel_all_orders/
close_position 全部真实代码路径，不新增生产代码。

**决策逻辑离线测试**（test_verify_fire_logic.py，7 用例，桩 ccxt 同
test_okx_adapter_safety.py 思路，patch time.sleep 保证 0.02s 内跑完）：
触发+归零+无反向→True / 触发+反向持仓→False（reduce-only 失守判定）/
超时未触发→None（不确定非失败）/ 触发但算法单未消失→False / 开仓失败
直接返回不进清理 / 止损创建失败仍触发 finally 清理不留裸仓 / CLI 拒绝
--fire+--side both 的模糊方向组合。验证「代码看到某种交易所响应会不会
下对结论」，与真实触发的等待时长无关，和用户的实盘实测互补而非替代。

本机 176 + 依赖版 74 全绿（discover+倒序，5s 内跑完），pyflakes 归零。
**待验证**：用户在 LTCUSDT 上跑 `verify_okx.py LTCUSDT 1 --side long --fire`
获得实弹结果后，止损三防线的「触发执行行为」环节方可视为闭环。


---

## 实弹触发验证通过（LABUSDT，止损三防线闭环）

用户用 `verify_okx.py LABUSDT 50 --side long --fire` 在活跃品种（LTCUSDT 两次
300s 超时未触发后按建议切换）上完成实弹触发验证，**约 10-15 秒内自然触发**：

- 触发后持仓精确归零、未反向——`--fire` 模式设计要验证的核心命题
  （reduce-only 语义在触发瞬间的真实执行行为，此前完全依赖平台承诺）通过；
- 算法单正确从待触发列表消失（False，非 mismatch/残留）；
- **附带验证**：`close_position` 的幂等防护首次在真实触发场景下生效——
  cleanup 阶段查询到已无持仓，正确跳过冗余平仓指令并返回 `already_closed`，
  证实"触发后 finally 清理"与"止损自愈巡检发现已平"两条路径不会互相踩踏。

至此止损三防线（账本 fail-closed / 验证式撤单 / 止损自愈巡检）的**全部环节**
均已获得实盘实证，包括此前从未测试过的「触发执行瞬间」。未做 short 方向的
独立实弹测试——reduce-only 机制方向对称、且已在 run_side 的双向非触发测试中
验证过挂单/撤单对称性，如实声明为收窄但非零的观察项，不影响本轮闭环结论。

本轮无代码变更（纯验证），仅留档。

---

## 全仓再审·第六轮（Fable 5：币↔张浮点往返实证缺陷 + 上轮保留项一并消除）

用户命题「逐字逐句审查全仓，实事求是」。逐行重读全部生产模块、前端、CI、文档，
并对疑点做数值实验证伪/证实后，本轮修复按风险从高到低：

**正确性修复（3）**：
1. **币↔张浮点往返截断丢一张（P1，数值实证）**：账本币数 = 「整步长张数 × 面值」，
   所有下单路径拿它再除回面值——IEEE 754 下 `(n × cs) / cs` 常得 `48.999…99` 级
   下侧尘埃，ccxt TRUNCATE / floor 兜底截断后**少一张**（实验：cs ∈ {0.0001…100}、
   n ≤ 20000 的组合约 2.4% 中招）。因换算确定性，交易所侧自洽不留残仓，但实际仓位
   恒比账本小一张：盈亏/成交后风险校验/持仓汇总系统性偏差，`round_quantity`
   「与真实下单张数一一对应」的分层契约被违反；n=1 时甚至截成 0 张误弃开仓。
   修复：`_coin_to_contracts` 截断前把张数吸附到最近步长（容差 1e-6 步；尘埃仅
   ~1e-9 步级，真实定价不会贴边），一处修复覆盖开/平/止损/翻转全部调用点，
   真实的不足一步部分仍按原语义截断丢弃。
2. **账本形状校验缺口（P3）**：`json.load` 成功只证明是合法 JSON——顶层被误改成
   数组、关键段类型错误的文件此前放行，运行中以裸 KeyError 崩溃，绕过 fail-closed
   的人工修复指引。现形状非法与解析失败同等对待（走备份恢复→仍失败拒启）；
   缺键按默认补齐（兼容老账本）。
3. **周期校验与 K 线供应能力脱节（P3）**：PERIOD_MAX=500 允许配出「所需 1000 根
   已收盘K线」的周期，而 OKX 单次最多 300 根（ccxt 不分页）——该品种每日警告、
   永不交易，fail-loud 落在运行时而非配置入口。新增 `ensure_candle_supply`
   （海龟 ≤297 / EMA长 ≤149 / EMA止损 ≤298），启动校验与 API 改参同源接入。

**上轮保留项消除（用户命题）**：`get_position` 重试装饰器叠在下单超时确认循环内层，
最坏等待成倍拉长（每次查询 1+2+4s 退避 × 15s 超时 × 循环 3 次）。拆出无重试的
`_get_position_once` 供确认循环与 `_position_contracts` 使用——循环自带「3 次 × 2s」
节奏，内层不再叠加；常规读路径 `get_position` 保留 3 次退避语义不变。

**文档与真实行为对齐**：`check_reentry_condition` docstring 声称「重入前提：EMA短期
仍高于EMA长期（多头环境）」，实际任何 EMA 关系都按当前方向重入（可反向，符合
「永远在市」设计）——仅两条 EMA 精确相等才不重入。docstring 与 signal_handlers
「EMA方向已变」的误导日志改为如实措辞（行为零变更，防止下个维护者建立错误心智模型）。

**低危加固与打磨**：atomic_write_json 补目录 fsync（rename 持久化，平台不支持时
静默降级）；wsgi 锁文件移出 /tmp 进项目目录（共享主机可预测路径可被本地用户抢占
致拒启）；/api/logs 改流式 deque 尾读（不再整读 10MB）；/api/trades 默认回最近
500 条（史书只增不减，汇总统计仍全量口径）；登录防爆破字典封顶 1 万条（触顶清
非锁定期条目，防伪造 IP 刷成无界内存）；品种出池且无持仓时清理 signal_states
（防随历史品种无限增长）；删除无调用方的 turtle `calculate_channels`；前端品种
策略缺省口径与后端对齐（缺 strategy 字段后端按海龟托管，前端原显示成双均线）。

**本轮复核后维持不动**：ProxyFix 默认单反代（运维清单已文档化，属部署纪律）；
trades_summary 全量扫描（聚合统计需要完整历史）；`_migrate_okx_legacy_state` /
调度幂等链 / 止损三态判定——逐项推演无缺陷，不为改而改。

新增测试 16：币↔张吸附 4（整张尘埃/floor 兜底/真实小数仍截断/分数步长）+
确认循环单次查询 2（不重试/常规路径保留重试）+ K 线供应能力 3 +
账本形状 5（数组拒启/段类型拒启/缺键补齐/备份恢复/双坏拒启）+ 信号状态清理 2；
另在启动烟雾测试的非法配置清单补 2 个超供应能力拒启用例。
验收：py_compile 全源 ✓；node --check app.js ✓；本机 192 用例全绿。
